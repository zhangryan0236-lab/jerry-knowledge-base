---
title: "黑马Vibe Coding零基础入门，vibecoding项目，涵盖Claude Code、Cursor、Codex、SDD、LangChain、Agent开发"
url: "https://www.bilibili.com/video/BV1RFTc62EaK/?p=22"
bvid: "BV1RFTc62EaK"
cid: "39531447360"
author: "黑马程序员"
upload_date: "2026-07-01"
subtitle_lang: "中文"
created: "2026-07-20"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116838327388595&bvid=BV1RFTc62EaK&cid=39531447360&page=22&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

本套视频教程所有配套资料领取方式如下：
关注黑马程序员公 粽 号，回复关键词：260701
【AI大模型学习路线图】展开查看更多内容
https://www.bilibili.com/opus/1129722427782201345
如何下载资料
https://www.bilibili.com/opus/443715248901563958

AI大模型开发热门教程：
AI大模型开发：BV1h1VbzHER2
Python语言进阶：BV1U2WmzfEqp
Python数据分析：BV1ReshzoEgG
机器学习：BV1Fzszz4Ek7
深度学习与神经网络：BV1c5yrBcEEX
NLP自然语言处理：BV1GByoBfE73
AI-OpenCV图像处理：BV1Fo4y1d7JL
人工智能深度学习：BV1L4411v7fB
图像与视觉处理：BV1Rz4y1R7Mb
========================================================================
本课程采用Vibecoding/SDD+实战案例+动手开发的教学模式，先拆解Agent的核心运行机制、Skill与Subagent的核心逻辑，再通过多个实战案例，一步步演示自定义Skill的开发、Subagent的任务拆解、复杂Agent的构建，每个案例都可直接复用，学员可快速落地自己的Agent应用。
========================================================================

## 字幕

`00:00` 哈喽小伙伴们大家好
`00:02` 欢迎回来
`00:02` 接下来让我们进入到课程的第四部分
`00:05` long chai g项目实战
`00:07` 那么到目前为止啊
`00:08` 我们关于web coding氛围编程呢
`00:10` 它的一些基础知识都掌握的差不多了
`00:13` 那么最后呢让我们来做一个AA领域
`00:15` 比较火热的RAG项目
`00:17` 那借助于这个项目
`00:19` 我们把氛围编程给它全部都落地了
`00:22` 好那我们的项目做什么呢
`00:25` 小张老师在今年1月份的时候
`00:27` 在B站发布了一款新的视频
`00:29` 叫做long char g大模型开发实战
`00:32` 那这款视频呢主要是通过long cha技术框架
`00:36` 去围绕着我们AI领域比较火热的RG技术
`00:39` 以及IG的技术去开发了相应的实战项目
`00:43` 好那这套视频啊里面的代码呢都是古法编程
`00:47` 通过手写一行行给它写出来的
`00:49` 那现在呢我们既然是web coding氛围编程
`00:53` 那所以我们的目标就是通过web coding
`00:56` 以零基础小白的视角
`00:58` 去完成一个IG项目的实战开发
`01:01` 我们可以给自己一个假设
`01:03` 哎比如说我是一个在校的大学生
`01:05` 但是呢我其实技术上并不是很好
`01:08` 很多东西我也都不懂
`01:09` 但是呢我的毕设就是要通过long unchain
`01:11` 框架去完成一个RIG的一个项目
`01:14` 那所以说我们就可以正好借助于web coding
`01:18` code code来去帮助我们完成项目的开发
`01:21` 好那尽管是小白视角
`01:23` 但是对于RIG这个基本概念呢
`01:25` 还得给大家去简单的普及一下啊
`01:27` 那IG它称之为叫检索增强和生成
`01:31` 其实主体上就分为两条线
`01:33` 一个叫离线处理
`01:34` 一个叫在线处理
`01:35` 那RIG啊本质上来说呢
`01:37` 你可以认为就是去提供知识库
`01:40` 那我们的模型啊
`01:42` 它其实本质来说很多问题都能够回答
`01:45` 但是他不是说所有的问题都能回答的
`01:48` 比如你问模型诶
`01:49` 我们学校哎
`01:51` 比如说某个班级
`01:52` 他的班主任是谁
`01:54` 那这个模型他可能就不知道了
`01:56` 又或者说你问模型
`01:57` 我现在心里在想什么
`01:59` 那模型肯定也不知道
`02:00` 所以说模型能够知道的都是来自于互联网
`02:04` 公开的数据
`02:05` 所以你问他一些公开的知识他都懂的
`02:08` 但是一些私有的知识
`02:10` 私有的信息呢
`02:11` 模型是不清楚的
`02:12` 那么为了解决模型的这个问题呢
`02:15` 就扩展出来了RIG的对应的技术站
`02:18` 那简单来说就是给模型提供外挂的知识库
`02:22` 哎你要问的问题
`02:23` 模型本身不懂
`02:24` 但是呢我给你一个参考文档
`02:26` 那模型看了参考文档它就会懂了
`02:29` 唉比如说我问一下唉
`02:31` 我们学校里面某某班的班主任是谁
`02:34` 模型肯定不知道
`02:35` 但是呢我顺手甩给模型一张来
`02:37` 这是我们学校里面的职工手册和职工名单
`02:41` 唉我把这些名单给了你
`02:43` 我再问班主任是谁
`02:44` 那模型不就知道了吗
`02:46` 所以这就是典型的一个知识库啊
`02:48` 通过知识库去扩充模型能够回答的内容
`02:52` 但是呢你直接扔给模型知识库
`02:55` 那效果肯定不好的
`02:56` 因为某些知识库的文档
`02:58` 比如我们以PDF来说
`03:00` 假设这个PDF它非常的大
`03:02` 有一个GB那么大
`03:04` 你把这一个GB的文件你扔给模型
`03:07` 那模型它光处理可能就处理不过来了
`03:10` 就这一个文件就够模型吃了
`03:13` 所以你的问题可能就提不出去了
`03:15` 那为了解决这个问题的话呢
`03:17` 很多时候我们要对知识库里面内容呢去做分段
`03:20` 然后去做匹配
`03:22` 做检索
`03:23` 唉我的问题只命中知识库里面的
`03:26` 或者说只命中这个参考文档中的某一段
`03:29` 哎我把这这一段提交给模型就可以
`03:32` 那所以那所以基于这个原因呢
`03:34` 就诞生出了RIG的相应技术
`03:37` 那IG的线性技术其实本质上就是两条线
`03:40` 离线和在线
`03:41` 那离线说白了就是为模型准备知识库
`03:45` 那离线流程我们可以看到图中
`03:47` 简单来说就是我们有非常多的一些知识库
`03:50` 或者说你可以认为是书籍啊
`03:51` 教材啊
`03:52` 领域文档或者企业报告
`03:54` 那这些文档可以大也可以小
`03:57` 但是呢我们很难把这些文档
`03:59` 整体都给模型作为参考资料
`04:01` 那是为了方便用模型
`04:03` 精确的找到他要参考的信息
`04:05` 所以我们要把这些东西的内容给它提取出来
`04:07` 比如转化为字符串
`04:09` 也也就是转化为纯文本
`04:11` 然后转化为纯文本之后呢
`04:13` 我们还需要给它划分为段落的区块
`04:17` 也就是划分为一片一片的小方块
`04:20` 唉
`04:20` 就是一段一段的把它划分成一段
`04:23` 一段之后的话呢
`04:24` 我们要把它进行向量的嵌入
`04:26` 最终去给他存入到向量库里面
`04:30` 那所谓的向量库啊
`04:32` 在专业领域上来说呢
`04:33` 可能解释时间比较长
`04:34` 我们就简单认为当我们存成向量之后
`04:37` 我们就可以靠它去做相似匹配了
`04:41` 也就是可以通过语义啊
`04:44` 就是你所提问的这一句话
`04:46` 它的含义
`04:47` 通过这个含义去知识库里面寻找匹配的段落
`04:51` 那这样的话如果你把它向量化了
`04:55` 就方便于我们从这个知识库里面
`04:57` 快速的抽取符合条件的段落
`05:00` 而不是把整个知识库都给模型
`05:02` 所以这个就是它的离线流程
`05:04` 简单来说就是上传知识
`05:06` 然后把这些知识划分成文本区块
`05:08` 然后把每一个块呢都给他存入到向量库里面
`05:11` 所以这一步你可以认为就是离线流程
`05:14` 就是往向量库里面去存就可以了
`05:17` 你都给他存进去就行了
`05:19` 那在线流程呢其实就是用户的一个工作流程了
`05:22` 比如说用户真的来提问了
`05:23` 那用户比如提问
`05:25` 他说产品A的使用流程是什么
`05:27` 这就是用户的真实问题
`05:28` 那模型如果不知道这个问题的答案
`05:30` 那么他就可以去知识库里面去找参考资料
`05:33` 那参考资料这句话就会被进行向量嵌入的动作
`05:37` 会得到一堆的向量
`05:39` 那这些向量的话
`05:40` 你就可以在向量库里面去做相似的匹配
`05:43` 通过你的语义
`05:44` 从库里面找到符合语义的参考资料
`05:47` 然后把这一个参考资料一段或两段啊
`05:51` 很小的片段
`05:52` 把这些小片段呢提供给模型
`05:55` 把你的问题和参考资料一块提给模型
`05:59` 那么模型就可以形成比较符合要求的回答了啊
`06:03` 所以这就是RIG的一个基础的概念啊
`06:05` 那所以这是它的在线和离线流程
`06:08` 那我们本次项目就以某一个商品
`06:11` 也就是某一个衣服的一个问答为例啊
`06:14` 我们围绕着衣服的一些属性呢
`06:15` 去构造一些本地的知识
`06:17` 那我们的使用者呢可以自由的更新本地知识
`06:20` 相当于就是自由更新知识库
`06:22` 然后用户去问模型的时候
`06:25` 也可以从这个知识库里面找到参考资料
`06:28` 那这里面其实本质来说就是两套流程
`06:30` 离线和在线
`06:32` 离线就是怎么样去管理这些知识
`06:35` 比如本地支持文件的加载和读取
`06:37` 怎么去做文本切分
`06:39` 怎么样存到向量数据库里面
`06:41` 那在线流程呢就是用户的提问拿过来了
`06:44` 怎么把用户的提问向量化
`06:45` 然后怎么从向量库里面去匹配合适的参考资料
`06:49` 然后呢怎么样把参考资料和用户提问打包
`06:52` 形成最终的一个pom的提示词去问模型
`06:55` 由模型生成答案好
`06:58` 那这是我们的一个前期的项目的一个
`07:00` 基础的了解啊
`07:02` 我们大体上知道我们要做的是一个知识库
`07:04` 问答的一个企业级的项目
`07:06` 那主要针对的呢就是商品
`07:08` 或者说衣服的一个问答流程
`07:11` 帮助于消费者在购买衣服的时候
`07:13` 唉
`07:13` 他提问提问的是关于这个衣服的相关东西
`07:16` 唉
`07:16` 我们的模型能够更加精准地回答用户的需求好
`07:21` 那了解了我们的基础的要求之后的话呢
`07:24` 我们就可以来着手把这个项目呢去给他开干了
`07:27` 那当然了
`07:28` 作为一个小白视角
`07:29` 我们并不能说上来就让模型帮我们写代码
`07:31` 我们肯定要进入到control code了
`07:33` 一个plan模式
`07:34` 我们先跟他聊聊规划
`07:36` 聊聊细节
`07:37` 我们先跟他聊一聊
`07:39` 聊通了再让他干活
`07:41` 因为毕竟我们以小白来看
`07:43` 我们可能技术上也不会
`07:45` 我只是说我的毕设就要用long cha
`07:47` 去做一个对应的项目而已
`07:49` 但其他的我都不懂
`07:51` 在这个过程中
`07:51` 我肯定要和cloud code好好对话一下
`07:54` 你得给我解释清楚
`07:56` 我明白了
`07:56` 你才能够动手干活
`07:59` 好OK那所以说现在第一件事情
`08:01` 我们就是应该先把我们的工程
`08:03` 或者项目的文件夹先创建出来
`08:06` 然后加载我们的cloud code
`08:08` 然后开始呢对它进行提问
`08:10` 先跟他好好的去沟通一下我们的需求
`08:13` 以及让他给我们产出来行动的计划
`08:17` 好的
`08:18` 那么回到了我们的电脑中呢
`08:20` 我们首先的话在我的D盘啊
`08:21` 就创建一个新的文件夹
`08:22` 作为我们项目的文件夹
`08:23` 我们给它起名叫做long char i g项目
`08:31` 好
`08:31` OK那有了这样一个文件夹之后的话呢
`08:34` 我们双击进入啊
`08:35` 那这样的话就有了一个工程文件夹
`08:37` 这个工程文件夹的话呢
`08:38` 我们就可以用vs code去打开它了
`08:40` 所以呢所以说我们来打开vs code啊
`08:44` 打开之后的话
`08:44` 默认它会进入到我们之前做的黑马这个项目
`08:48` 那没关系啊
`08:49` 我们在这里面可以看到
`08:50` 是不是有个叫打开文件夹的按钮
`08:51` 又或者说在上面文件
`08:53` 这里又有一个叫打开文件夹的按钮啊
`08:55` 我们重新换一个文件夹
`08:57` 打开打开我们的long unchain rg项目的文件夹
`09:00` 来选择文件夹
`09:01` OK那此时我们的vs code就切换项目文件夹了
`09:04` 然后选择信任此作者
`09:06` 好的
`09:07` OK那这样的话我们就进来了
`09:08` 然后这里面它默认会进入到啊
`09:11` vs code自带的一个agent的聊天工具
`09:13` 我们我们不需要它
`09:14` 我们还是回到我们的coud code中就行了啊
`09:17` 回到我们的可乐code
`09:18` 然后的话默认他会开一个新的会话
`09:20` OK我们就基于这个会话来去进行对应的工作了
`09:24` 好那跟我们前面一样
`09:25` 首先的话我们先给他选择到plan模式
`09:28` 计划模式
`09:29` 然后先跟他好好的去沟通一下
`09:31` 那沟通的内容的话呢
`09:33` 字比较多
`09:34` 我已经提前写好了
`09:35` 到时候大家也可以从我们的语雀文档中
`09:38` 把这些内容呢给他复制过去
`09:39` 又或者说你对着自己敲一遍
`09:41` 其实也没有多少字啊
`09:42` 我们简单看一下
`09:43` 假设我是个大学生
`09:44` 我要做毕设哎
`09:46` 我就听说long cha很是很知名
`09:48` 所以我的毕设呢就是通过long cha框架去做一个
`09:51` IG企业级知识库问答系统
`09:53` 那么需求呢
`09:54` 就是用户可以通过浏览器去进行知识库
`09:57` 然后得到问答的操作
`09:59` 那问答系统主要是针对电商平台
`10:01` 售卖商品进行回答
`10:02` 提供的知识库
`10:03` 多数呢就是商品相关信息
`10:05` 然后帮助用户啊
`10:06` 帮助更好的回答用户关于商品的一些问题
`10:09` 那技术选型上哎
`10:11` 我只知道long茜
`10:11` 或者说我只听说过狼茜
`10:13` 所以必须选择long cha这个框架
`10:15` 那其余的技术站哎我作为小白
`10:17` 我也不知道
`10:18` 你来决定就行了
`10:19` 比如说你的前端用什么随便啊
`10:21` 你反正你自己定就可以
`10:22` 你给我个方案
`10:23` 我自己选
`10:24` 然后呢下面是我要有我有有我想要的功能
`10:27` 比如我们需要能够支持用户在浏览器中进行
`10:31` 知识库的管理啊
`10:32` 由于你是一个知识库的一个问答系统
`10:35` 那么知识库管理肯定是必要的对吧
`10:37` 所以你必须要给我提供知识库管理的功能
`10:40` 那第二个能够支持用户在浏览器中
`10:42` 和模型进行问答的功能
`10:45` 哎这个问答功能是我们的核心功能
`10:47` 是我们提供给用户的一个必要的东西啊
`10:49` 那同时问答的时候要尽量引用知识库的内容
`10:52` 作为参考
`10:53` 那么你要不用知识库
`10:55` 那我要构建它干嘛呢
`10:56` 我直接给模型干不就行了吗
`10:58` 所以你必须要去尽量去引用知识库的内容
`11:00` 来去作为参考
`11:01` 在回答中
`11:02` 如果你引用了知识库
`11:04` 那么你应该把它显示出来
`11:05` 唉你这你这个回答引入了知识库中的哪个片段
`11:09` 你要给我显示出来啊
`11:10` 这是我的需求
`11:11` 第三我们要能够支持多用户多绘画管理
`11:14` 每个用户都有独立的绘画
`11:16` 哎我不能说只允许一个用户对吧
`11:18` 你可以去多个用户一块用
`11:20` 没问题
`11:21` 那同时呢还要能够保持用户的会话记录
`11:24` 就是用户不不管什么时候进来
`11:26` 他都能够登录后找回它的历史对话好
`11:30` 那既然能够涉及到多用户
`11:32` 还能找回历史对话
`11:33` 那肯定用户需要管理的
`11:34` 所以这里面也需要支持基础的用户注册
`11:37` 登录功能
`11:38` 支持用户修改密码
`11:40` 然后同时呢我们自己呢给自己设个管理员
`11:43` 我们先说管理员用户名admin密码13456
`11:45` 然后只有管理员才能去做知识库管理
`11:48` 那这个是肯定的对吧
`11:49` 企业内部的知识库我不可能放开给普通人互送
`11:52` 我只有管理员才能去搞这个知识库
`11:54` 去管理知识库
`11:55` 那其他其他普通用户
`11:57` 你就进行知识库的问答就行了
`11:59` 然后第七点我们还做一个额外要求
`12:01` 你不仅仅要实现基础功能
`12:03` 也要做各类技术的一个性能优化
`12:05` 唉你要用各种各样的技术
`12:07` 你帮我做优化
`12:08` 但是怎么优化或者用什么技术
`12:10` 我不太我不太了解啊
`12:11` 我作为一个小白
`12:12` 我不知道
`12:12` 但我就有这个要求
`12:13` 因为我要做B毕设答辩对吧
`12:15` 我肯定也得有亮点
`12:16` 我不能说只是实现一个long unchai g对吧
`12:18` 我这个技术要有亮点
`12:20` 从而去达到企业及使用效果
`12:22` 所以这一个让模型来帮我们去想
`12:24` 那第八个就是哎我作为一个小白
`12:26` 我可能就只想到这么多了
`12:28` 那剩余的我也想不到
`12:29` 那模型呢你就自己评估一下
`12:32` 你看看还有哪些可以添加的
`12:33` 添加的功能点
`12:34` 你都可以给我讲一讲啊
`12:35` 我们也去沟通一下
`12:36` 当我们说你帮我列出来方案计划
`12:39` 我们先讨论一下
`12:40` 哎先不要干活
`12:41` 先讨论为主啊
`12:43` 所以这是我们当前的提示词
`12:44` 也是和我们coud code进行沟通的一个基础啊
`12:47` 好那我们先CTRLC先进来
`12:49` 先跟大家简单的聊一下
`12:51` 好复制粘贴
`12:52` 然后运行
`12:53` 让他来帮我们去完成这个毕设的一个行动规划
`14:23` 好的
`14:24` 那么经过一段时间等待的话呢
`14:26` 他已经给我们把这个计划列出来了
`14:29` 那左边就是他即将要写入的计划啊
`14:31` 我们可以来看一看
`14:32` 来好技术方案上下文里面描述了毕业设计项目
`14:36` 基于long cha框架构建企业级rag系统
`14:38` 然后通过浏览器进行知识库管理啊等等
`14:41` 然后技术站总览啊
`14:43` 大语言模型使用欧拉玛加千问2.57B啊
`14:47` 免费本地运行中文优化答辩
`14:49` 可离线演示啊
`14:50` 也就是说他需要在我们电脑里面去部署欧拉玛
`14:53` 然后去跑千问2.57B好
`14:55` 然后嵌入模型是跑这个BGE杠M3好
`14:59` 然后re rag框架呢就是long chain
`15:01` 加加上这个long chain community
`15:03` 然后再来后端框架fast API啊
`15:05` python3.11啊
`15:07` 这个也算是比较知名的前端框架
`15:09` 使用react18加typescript啊
`15:12` 这个也没啥问题
`15:12` 都算是比较火的框架
`15:14` UI使用这个叫做n t design5啊
`15:17` 这个的话是一个UI框架
`15:19` 然后状态管理向量库
`15:20` 关系型数据库
`15:21` Circle
`15:21` light pd pd green circle可选
`15:24` 然后OM选择circle army啊
`15:26` 然后认证还有像文键的解析
`15:29` 还有像部署docker compose好
`15:32` 那这是它里面给我们
`15:33` 所以所演示的一个全部的技术框架
`15:35` 那其实在这个过程里面还算是比较比较详细的
`15:38` 那我以专业人员的视角来看
`15:40` 其实已经是非常非常企业化了
`15:43` 那同时这里面还涉及到了docker部署
`15:45` 那这里面就要求我们本地人
`15:47` 可能还需要去安装部署docker的环境好
`15:51` 那所以这个docker的话
`15:52` 我们等会再看
`15:52` 我们先继续往下面跑啊
`15:54` 为什么选欧拉玛
`15:55` 而不是选择原片啊
`15:56` 零费用零费用适合闭塞反复测试
`16:00` 然后离答辩时可以离线演示
`16:02` 也就是说他要让模型泡我们本地模型
`16:04` 那其实这个倒也不是不可以啊
`16:06` 那只不过对大家电脑呢是有要求的
`16:08` 你像我电脑的配置啊
`16:10` 是有独立显卡的
`16:12` 然后呢是有这个叫4060的一个独显
`16:15` 然后8GB的独显
`16:17` 那也就是说我的电脑
`16:18` 其实这个配置是支撑的起来
`16:20` 偶拉玛的一个本地部署啊
`16:22` 但是对于大家来说就得看了
`16:24` 如果说你们的电脑没有独立显卡
`16:27` 没有英伟达的独立显卡啊
`16:28` 那啊或者说呃不管英伟达还是AMD
`16:31` 反正都有对应的技术方案
`16:33` AAMD也能跑
`16:33` 但最少你得有个独立显卡
`16:35` 而且呢最少有个4G
`16:37` 乃至呃最好是8GB的一个显存
`16:39` 那如果说没有这个硬件的话
`16:41` 那可能还是要选择云端i pi啊
`16:44` 比如选择阿里云或者就选择DC也都是可以的好
`16:48` 那我们先顺着他的这个流程往下面再来看啊
`16:51` 这个欧拉玛我们等会待选啊
`16:52` 待选再来看下系统架构
`16:54` 系统架构的话呢
`16:55` 浏览器客户端在这里面
`16:57` 我们选择的是fast的API后端啊
`16:59` 这个应该也没啥问题
`17:01` 然后circle light呢去做这个数据库
`17:03` chroma dB去做这个向量存储
`17:05` 欧拉马去做模型服务好
`17:08` 然后这里面主体上大部分都是AH
`17:10` fast API的一些后端的一些动作
`17:12` 比如说中间件层
`17:13` 认证层
`17:14` 还有API路由层
`17:15` 还有我们的rag管道啊
`17:16` 这一系列都由这个fast i API去做缝合啊
`17:19` 数据模型
`17:20` 它给我们描述的主体要有的几张核心表
`17:23` 用户的一些表
`17:24` 消息的一些表
`17:24` 还有知识库文档的一些表
`17:26` 然后向量库的话呢选择了一个叫做啊
`17:28` 叫做啊connection
`17:30` 就是我们数据集合啊
`17:31` 选择chroma dB去做了一个下单库
`17:33` 那IAPI设计设计了这些IPI
`17:36` 然后用来去做注册登录
`17:37` 修改密码
`17:38` 还有像绘画管理等等
`17:40` 还有像问答知识库管理这些API
`17:42` 那可能作为小白视角
`17:43` 我们可能看不懂啊
`17:44` 大体上过一眼就行了
`17:45` 然后rag的话呢文文件设置管道
`17:48` 我们可以看一下它的主体流程
`17:49` 就是文件上传类型
`17:51` 检测文档加载器分发
`17:53` 也就是去分去分类你的文文件
`17:55` 是文本文件还是PDF文件还是CSV啊等等
`17:59` 然后呢
`17:59` 通过不同的文档加载器去加载这个文档好
`18:02` 加载好之后的话呢去把它内容清洗一下
`18:05` 然后呢去对它进行一个文本的一个分片啊
`18:09` 分片分片完之后的话呢
`18:10` 去对它去做原数据丰富
`18:12` 就是丰富它的来源类别
`18:14` 产品名
`18:14` 然后做驱虫
`18:15` 做批量的向量化
`18:16` 最后到向量库存储好
`18:19` 那以专业视角来说
`18:20` 这个是没问题的
`18:21` 那以小白视角来说哎无所谓了
`18:22` 看不懂
`18:23` 反正跑一跑试一试没问题就可以好
`18:25` 那再来看检索管道入户的问题
`18:28` 预处理去做向量化
`18:29` 去做余弦相似度搜索
`18:31` 然后再来去说上下文组装啊
`18:33` 这个也是没啥问题的啊
`18:34` 包括生成管道
`18:34` 还有下面的这个叫前端页面与路由啊
`18:37` 还有像性能优化
`18:38` 我们主要来看看性能优化
`18:39` 它帮我们做了哪些
`18:40` 比如说流式响应哎
`18:42` 做了一个叫逐token推送哎
`18:44` 体感比较好
`18:45` 还有对话历史窗口只加载十条教育教育上下文
`18:48` 避免卡tag溢出哎
`18:49` 这个没问题
`18:50` 批量下的话
`18:51` 还可以去批量去进行向量摄入的速度可以提升
`18:54` 还有也给我们增加了查询的缓存
`18:56` 可以避免一些高频问问问题
`18:59` 问问题的一个极致响应啊等等
`19:01` 还有像异步IO
`19:02` 还有像前端防抖
`19:03` 这里面也做了不少的性能优化点
`19:05` 那如果说作为一个毕设的话呢
`19:07` 其实你可以让我们的AI帮你去整理好这些东西
`19:10` 专门你去阅读理解一下
`19:12` 毕设的时候就可以很好发挥了
`19:14` 好还有像附加功能点
`19:16` 答案反馈建议问题引用高亮哎
`19:18` 对话导出
`19:19` 这都是增强体验的
`19:20` 还有毕设的加分项
`19:22` 多格式批量导入啊
`19:23` 知识库健康看板
`19:24` 暗色模式等等啊
`19:25` 所以整体上来说
`19:26` 我们大体过了一下流程和技术站上
`19:29` 还算是没啥问题的
`19:31` 是可以跑的
`19:31` 但是有两个点呢
`19:33` 我们是需要跟他去沟通一下的
`19:35` 第一个就是我的模型啊
`19:36` 我的电脑是可以跑欧拉玛的
`19:38` 但是大家同学啊
`19:40` 屏幕前的同学们电脑不一定都百分百能跑
`19:43` 所以我们这边还是要跟他去简单的沟通一下
`19:46` 我们说唉还是选用云端的API模型吧
`19:49` 那当我们选中云端的API模型的话呢
`19:52` 不管你的电脑配置是什么样的
`19:53` 都能够完全带起来
`19:54` 因为想要跑本地模型
`19:56` 必须要高配电脑啊
`19:57` 那个低配电脑肯定带不动了
`19:59` 所以我们还得选择云端模型
`20:00` 那云端模型的话
`20:01` 其实我们当前已经接入了deep seek
`20:03` 但deep seek它在内部
`20:05` 没有与提供一些做文本向量相关的模型
`20:09` 它其实就提供了一个deep sk v4pro和V4flash
`20:11` 而是一个通用化的模型
`20:13` 那所以对于我们来说
`20:14` 如果想要去做一些向量啊等等
`20:16` 还是需要去找另外的一个模型服务商
`20:19` 那一般来说用的最多的就是阿里云
`20:21` 那所以等会给大家演示一下
`20:22` 去阿里云去注册
`20:24` 去获取IPIK
`20:25` 你把那IPIK给到我们的模型就可以了
`20:27` 然后告诉他我要接入阿里云好
`20:30` 那这是第一点啊
`20:31` 我们要去走云的IPI啊
`20:33` 我们消耗一点钱倒没问题
`20:35` 主要是害怕什么
`20:36` 害怕我们的电脑带不动啊
`20:37` 这是第一点
`20:38` 那第二点的话呢
`20:39` 就是我们这个docker就不要去部署了
`20:41` 我们就直接在我们电脑里面直接部署就行了
`20:43` 我不需要使不需要通过docker去跑好看
`20:47` 那所以我们跟大家聊一下
`20:48` 我跟我跟大家聊一下
`20:50` 我说哎我们第一啊
`20:51` 我需要我需要选用云端模型模型啊
`20:58` 然后啊云端IAPI啊
`21:00` API因为因为啊我电脑带不动
`21:03` 我电脑带不动哎
`21:04` 然后我倾向于选择阿里云的百炼平台可以吗
`21:11` 啊这是第一个第二啊
`21:14` 我不用docker
`21:15` 不用docker啊
`21:16` 因为什么
`21:17` 因为网络原因原因基本下载不了
`21:23` 不来镜像啊
`21:24` 镜像所以说基本不可用啊
`21:27` 所以我要把这两个方案给他推掉啊
`21:29` 剩下的都都OK啊
`21:30` 其余都OK啊
`21:31` 其余都OK
`21:35` 好我现在给大家说一下
`21:36` 哎你把方案再给我重新思考一下啊
`21:38` 把这两个点呢去过去重新思考一下
`21:54` 好的
`21:54` 那此时的话呢他把我们的方案呢给更新完成了
`21:57` 那我们再来去重新看一下我们所关心的两个点
`21:59` 比如说在接入这里
`22:01` OK他已经给我们改成了阿里云百炼的大西
`22:04` SCOPPIPI好
`22:05` 然后这一个没问题
`22:06` 改好了之后
`22:07` 我们再来看一下
`22:07` 把启动方式从docker改成了我们start bat
`22:10` windows批处理脚本啊
`22:12` 这样的话就可以方便我们一键启动
`22:13` 不需要docker啊
`22:15` 那这样的话我们也能够正常的本地启动和答辩
`22:17` 演示都没有任何的问题啊
`22:19` 啊所以其他的话没有什么太多的变化
`22:21` 那我们就可以去告诉他
`22:22` 我接受这个这这个方案了
`22:25` 好但是接受之前的话呢
`22:26` 我先给大家去演示一下
`22:28` 比如说如何去注册和获取到
`22:30` 阿里云的百炼平台的API好
`22:32` 那这个的话其实跟我们去获取这个deep sk
`22:35` 主体上来说是差不多的
`22:36` 来首先我们来打开浏览器
`22:38` 好浏览器打开之后的话呢
`22:40` 我们来输入地址叫百炼点
`22:42` 阿里云点com啊
`22:43` 它的域名很简单
`22:44` 就叫百炼啊
`22:45` 百炼的拼音点阿里云点com啊
`22:48` 进来之后的话呢
`22:49` 就可以进到阿里云的一个百炼平台了
`22:51` 那正常情况下我们是需要先登录的
`22:54` 这个登录的话大家就自行注册登录就可以了
`22:58` 好这边的话我是有自己的账号了
`23:00` 所以说我就登录自己账号了
`23:02` 那没有账号的自行注册也可以
`23:04` 并且阿里云现在有一个福利
`23:06` 就是你注册新用户
`23:08` 那么它默认有三个月的免免免费的
`23:11` 而且很多模型都有100万的免费token的额度
`23:15` 其实足够于我们去做这个项目了
`23:17` 因为我们大量的思考消耗
`23:19` 在于我们这个可乐的扣的消耗的啊
`23:21` 其实你在这个RG项目里面消耗的并不多啊
`23:24` 所以说如果你新注册阿里云
`23:26` 还有三个月的免费时长
`23:28` 每个模型都有1000000token的免费额度
`23:30` 哎其实足够我们用了好
`23:32` OK那我来登录一下
`23:34` 好的
`23:34` 那么正常登录完成之后的话呢
`23:36` 大家一定要做一件事情
`23:38` 由于我是已经做过了
`23:39` 所以大家一定要做一件事情
`23:40` 就是把你的账号去实名认证一下
`23:43` 否则他是不允许你去调用它的API的啊
`23:46` 所以这里面必须要去实名认证
`23:48` 然后除此以外
`23:49` 可以看到这里面是不是有免费额度呀
`23:51` 其实在阿里云里面是提供了免费额度的
`23:53` 当然我这个账号注册的比较久了
`23:55` 有很多模型都没有什么免费额度了
`23:57` 但是呢我依旧是有一些模型有的
`24:00` 比如你像新推出的千万3.7plus
`24:02` 哎这个模型呢我就有免费额度啊
`24:05` 还有像这个什么千万3.5OCR
`24:07` 这些模型我都有免费额度的啊
`24:09` 都是默认100万啊
`24:11` 都是100万
`24:11` 而且过期时间呢是三个月的时间
`24:13` 那大家如果是新注册的账户
`24:15` 应该是所有的模型都是免费使用
`24:17` 每个模型100万额度用完了才会收费啊
`24:21` 所以说大家啊新注册账户还是挺有用的好
`24:24` 那这个模型免费额度回头再看
`24:26` 我们现在来看看
`24:27` 比如说怎么样去创建或者使用它的IPIK啊
`24:30` 那IPIK的话呢
`24:31` 首先你看这上面有个叫模型的按钮啊
`24:33` 点击这个模型啊
`24:35` 进来之后呢
`24:35` 在左边最下面有个叫APIK的地方啊
`24:38` APIK点击它
`24:40` 其实和我们当前那个啊deep sick的一样
`24:43` 你在这边就创建一个APIK用就行了
`24:45` 比如我创建一个APIK
`24:46` 然后这边这个业务空间不让选就不管了
`24:49` 比如这个就写写上一个描述
`24:51` 用于啊i g long chain
`24:54` 好
`24:54` 给他写上RNG long线
`24:56` 然后给它全部权限就可以
`24:57` 然后点击确定好
`24:59` 那此时我们的IPIK呢就生成好了
`25:01` 那这里跟我们deep sk一样
`25:03` 离开这个对话框
`25:04` 你就无法取回这个密钥了
`25:06` 所以你一定要给它复制好
`25:08` 说或者说给他点击这个下载好
`25:10` 我点击下载这API可就保存了
`25:12` 然后点击复制好
`25:13` 当你点击关闭
`25:14` 你要是没给他下载或者没保存好这个IPIK
`25:17` 你就再也找不到了啊
`25:18` 所以一定要注意啊
`25:19` 如果你丢了IPIK
`25:20` 那你只能再重新创建一个了
`25:22` 好OK那关闭之后的话
`25:24` 相当于我们的阿里云百炼呢
`25:25` 就已经有了这个NPIK了
`25:27` 好那我们给他啊
`25:28` 回到我们的vs code中好
`25:31` 那有了APIK之后的话呢
`25:33` 现在我们就可以去接受它这个方案了
`25:35` 来点击接受
`25:39` 好那他在这个过程里面呢
`25:41` 其实就开始去干活了啊
`25:43` 干活了
`25:44` 那在这个过程里面
`25:45` 他遇到一些命令需要创建啊等等
`25:47` 我们给他个yes就可以了
`25:50` 好那在这个过程中
`25:51` 其实我们可以去等一会
`25:52` 等他等他在提问的时候
`25:54` 其实我们可以来打断一下
`25:55` 因为他在后面
`25:56` 指不定在什么时候询问阿里云百炼的IPIK
`25:59` 他不知道对吧
`26:00` 所以等一会等他再有提问我们的时候
`26:02` 我们直接给他打断
`26:03` 然后呢就跟他说一声IPIK
`26:06` 我现在给你啊
`26:07` 就相当于是让他记住IPIK是啥
`26:09` 你在合适的地方给我记住
`26:10` 记住就可以了
`27:23` 好经过一段时间等待啊
`27:25` 可以发现他还是在写啊
`27:26` 那说明到目前为止
`27:27` 他就一直在给我们去进行代码的编写
`27:30` 那刚刚通过它的提示呢
`27:32` 我们会发现他告诉我们后端的其实已经写完了
`27:35` 那可以看到AI写代码的效率是真快呀
`27:38` 好他后端写完了
`27:39` 然后现在正在去写前端啊
`27:41` 那我可以简单看一下
`27:42` 你看这边你要back end
`27:43` 他已经噼里啪这一会啊
`27:45` 写了非常非常多的东西了啊
`27:47` 写了非常多样多东西
`27:49` 然后这边还有像test啊
`27:50` da chrome dB里面的IPPIPI啊
`27:53` 这些这些API代码啊
`27:55` 噼里啪啦都已经给我们写出来了
`27:57` 可以看到效率非常之高啊
`27:58` 好那前端的话呢
`28:00` 他现在是正在写前端啊
`28:01` 比如前段这里面API等等
`28:03` 也写出来了一部分的API的代码了
`28:05` 好那我们继续稍稍等待一下
`28:07` 等他把这些所有的代码呢去写完
`28:57` 好的
`28:58` 那么经过一段时间的等待之后的话呢
`29:00` 他现在又要执行相应的命令了
`29:02` 那这个命令其实本质来说是要执行npm inst
`29:05` 时道
`29:05` 也就是去安装安装前端的相关依赖
`29:08` 那如我们前面所说啊
`29:09` 我们在此时呢我们说哎我把IPIK先给你啊
`29:12` 我把阿里云的APIK啊先给你
`29:18` 然后你找地方放好放好
`29:26` 好然后然后呢
`29:28` 然后你继续啊
`29:31` 我在中间给他插一句啊
`29:33` 插一句
`29:33` 我把IPIK给你
`29:34` 然后剩下的你再继续啊
`29:35` 啊你把IPIK给我搞搞好
`29:37` 然后继续去做你未完成的事情就可以了
`29:44` 好了
`29:44` 然后他又开始回过来
`29:46` 要继续执行前端的相应的命令了
`29:47` 那我们给他个yes啊
`29:48` 允许后续全部的npm install相关的来允许
`31:53` 好那经过一段时间的编码之后呢
`31:56` 可以看到他现在基本上是已经把所有的代码呢
`31:58` 都已经写完了
`31:59` 那只不过现在一直在启动的测试中
`32:03` 那也就是说他的目的呢是要把我们的后端启动
`32:05` 然后启动完之后呢去试一试能不能跑
`32:08` 但是呢一直没有启动成功
`32:09` 那这也是正常的啊
`32:11` 写代码很难说一次性成功
`32:12` 不管是AI还是说我们人类写代码
`32:15` 都是启动的时候各种报错
`32:17` 然后一个个错误一个错误去解决掉啊
`32:19` 所以说我们在稍事等待他
`32:21` 一会等待他把这些啊bug呀问题都修复完之后
`32:24` 让他第一次成功启动啊
`32:26` 相当于我们第一个小阶段呢就搞定了
`32:32` 好了他所有的后端的都已经是全部验证通过了
`32:36` 现在正在去尝试着前端的一个构建啊
`32:39` 那我再来把一些命令给他允许了
`32:41` 让他把前端的去构建出来
`32:49` 好了
`32:49` 那此时他说我们的项目的
`32:51` 目前第一版算是基本上开发完成了
`32:54` 然后这里面还有一些这个启动的方式啊等等啊
`32:57` 他告诉我们双击这个叫使他的BT1键启动
`33:00` 然后访问这个叫5173的一个地址
`33:02` 好OK那我们来试一下
`33:04` 来我们CTRLC一下复制一下这一个地址
`33:07` 然后的话回到我们当前这个项目里面
`33:09` 看到有个叫TT点BT
`33:11` 我们就按他所说来双击运行它好
`33:13` 这样的话这样的话呢弹出来一个黑色的窗口
`33:16` 就是我们的命令行窗口
`33:17` 告诉我们按任意键继续好
`33:19` 我们按下这个键继续之后呢
`33:20` 然后再给我回过来
`33:21` 我再来访问他所说的叫5173来运行
`33:27` 好的可以看到他告诉我们拒绝访问
`33:29` 那说明我们的项目呢是启动失败了
`33:32` 那所以遇到这个问题也不用怕
`33:33` 我们就简单的给大家描述一下就行了
`33:35` 比如说哎我运行运行start点bet后啊
`33:41` 直接告诉我按任意键继续
`33:45` 然后按的任意键后
`33:49` 然后命令行退出了
`33:52` 然后打开网页啊
`33:54` 无法显示
`33:59` 好的这里面给了我一个API的400错误
`34:02` 那这个的话应该是来自于我们的deep seek
`34:05` V4pro模型本身的问题啊
`34:07` 不是cler code的问题啊
`34:09` 所以说在运行的时候
`34:10` 他并不是说百分百都能够
`34:12` 每次跟那个deep sk能够顺畅的沟通啊
`34:15` 可能会有极小的几率产生问题
`34:16` 那没关系啊
`34:17` 我们再来重新把我们的问题说一下就行了
`34:20` 由于我们这些绘画的话都是存在于文件里面的
`34:23` 所以你再次运行它还是能够续上去的好
`34:26` OK那我们再稍稍等待一下
`34:27` 让他帮我们把这个问题给我们解决一下
`34:33` 好的OK他说现在呢是修复完成了好
`34:37` 那我们就来再来试一下
`34:38` 来好双击打开啊
`34:43` 这里面你看他就给了我们一堆乱码
`34:45` 然后给我们输出这样的东西好
`34:47` 没关系啊
`34:47` 我们就有什么输出
`34:49` 就是有就给什么输出
`34:51` 然后我们直接去问我们的模型就可以了
`34:55` 好这是我运行后的运行start点BT的输出
`35:06` 啊这个也没有办法
`35:07` 主要是因为我们这个windows系统
`35:09` 很多时候在编程方面
`35:10` 我们都说LINUX比较强大啊
`35:13` 就在于你看windows系统
`35:14` 它这个CMD命令行方面确实会有很多的问题啊
`35:17` 你像我们这个叫b i at脚本
`35:19` 这典型的是中文乱码问题
`35:21` 以及一些路径的相关问题啊
`35:23` 所以说啊为什么程序员喜欢LINUX
`35:25` 很多时候在在LINUX上显而易见
`35:28` 非常简单的玩意
`35:29` 到windows上就总是会出现各种各样的问题啊
`35:31` 好OK那没关系啊
`35:33` 反正A还是还是很强大的
`35:35` 我们让他帮我们把这个问题给他修复一下
`35:37` 就可以了
`35:37` 好现在让我们再重新双击它
`35:40` 那我再来去试一下
`35:41` 我再信你一次来走
`35:42` 你来到这边双击它
`35:46` 好OK我有双屏
`35:47` 所以说很多信息都给我弹到这个屏幕里面来了
`35:50` 然后我们我都给他拉到这边好
`35:52` 双击打开之后
`35:53` 你看他一步一步检查环境
`35:55` 然后配置后端
`35:56` 配置前端启动完成啊
`35:58` 启动完成
`35:59` 然后这边又弹了两个页面啊
`36:01` 比如这边是我们的叫做VITE的一个状态
`36:04` 还有这个是当前启动了一个啊
`36:06` 8888000的一个端口服务好
`36:09` 然后问我们是否打开前端页面来给他个Y啊
`36:12` Yes
`36:12` 来给我打开浏览器
`36:13` OK好的
`36:14` 没问题
`36:15` 是不是一次运行起来
`36:16` 那我们来试一下我们的中户名
`36:18` 我们给他规划的中户名要ADD min
`36:20` 然后密码叫123456
`36:23` 来登录试一下好
`36:24` 然后保存一下
`36:25` Ok
`36:25` 诶
`36:26` 这边的话确实是有了一个对应的一个
`36:28` 基本的一个绘画页面
`36:29` 然后这边还有个叫hello
`36:31` 这是我们当前的绘画
`36:32` 然后还有知识库管理的动作
`36:34` 还有个人中心啊
`36:35` 管理员还有修改密码
`36:37` OK那可以看到我们当前本次的话
`36:39` 一些基本的功能呢算是差不多了啊
`36:43` 那后续的话我们肯定要对它进行深度的使用
`36:45` 去用一下测的里面有些什么问题
`36:47` 然后还要对它去进行
`36:49` 比如说代码的评审
`36:50` 要对他去执行单元测试
`36:52` 还要对它的代码质量做检查啊
`36:54` 还有很多的事情要做
`36:55` 但是我们现在第一版算是跑起来了
`36:58` 我们已经完成了一个基础的0~1的动作了
`37:01` 好嘞
`37:02` OK那这是我们当前项目开发的第一步
`37:04` 那我们先把这个视频保存一下
`37:06` 后面我们再来去进行后续的动作
`37:08` 好的
`37:09` 那么让我们下一个视频再见