---
title: "黑马Vibe Coding零基础入门，vibecoding项目，涵盖Claude Code、Cursor、Codex、SDD、LangChain、Agent开发"
url: "https://www.bilibili.com/video/BV1RFTc62EaK/?p=21"
bvid: "BV1RFTc62EaK"
cid: "39549929323"
author: "黑马程序员"
upload_date: "2026-07-01"
subtitle_lang: "中文"
created: "2026-07-20"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116838327388595&bvid=BV1RFTc62EaK&cid=39549929323&page=21&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

本套视频教程所有配套资料领取方式如下：
关注黑马程序员公 粽 号，回复关键词：260701
【AI大模型学习路线图】展开查看更多内容
https://www.bilibili.com/opus/1129722427782201345
如何下载资料
https://www.bilibili.com/opus/443715248901563958

AI大模型开发热门教程：
AI大模型开发：BV1h1VbzHER2
Python语言进阶：BV1U2WmzfEqp
Python数据分析：BV1ReshzoEgG
机器学习：BV1Fzszz4Ek7
深度学习与神经网络：BV1c5yrBcEEX
NLP自然语言处理：BV1GByoBfE73
AI-OpenCV图像处理：BV1Fo4y1d7JL
人工智能深度学习：BV1L4411v7fB
图像与视觉处理：BV1Rz4y1R7Mb
========================================================================
本课程采用Vibecoding/SDD+实战案例+动手开发的教学模式，先拆解Agent的核心运行机制、Skill与Subagent的核心逻辑，再通过多个实战案例，一步步演示自定义Skill的开发、Subagent的任务拆解、复杂Agent的构建，每个案例都可直接复用，学员可快速落地自己的Agent应用。
========================================================================

## 字幕

`00:00` 零基础学AI
`00:01` 月薪过万就来黑马程序员
`00:06` Hello
`00:06` 小伙伴们大家好
`00:07` 欢迎回来
`00:08` 本小节我们来学习在CD code中去配置hook
`00:11` 也就是钩子
`00:12` 那所谓的钩子呢其实就是一种拦截机制
`00:16` 那它可以在工具调用前
`00:17` 工具调动后
`00:18` 又或者说工具调动失败等场景下
`00:21` 对我们的动作去做拦截好
`00:24` 那我们来看看我们的需求啊
`00:26` 我们到目前为止呢去配置好了两个sub edit
`00:29` 也就是子代理
`00:30` 分别是用于单元测试的以及用于质量检查的好
`00:34` 那我们现在有个需求
`00:35` 就是每当我进行代码改动
`00:38` 那当我验证完通过之后的话
`00:40` 正常我会对它进行用git去打下来一个存档点
`00:44` 并且把这个存档点呢推送到我们的远程仓库中
`00:47` 好我们现在想要的就是你在打存档点
`00:50` 在打这个存档点之前
`00:52` 我有个要求
`00:53` 你必须要经过一次单元测试
`00:56` 以及经过一次质量检查
`00:58` 如果你没有经过这个动作
`01:00` 我是坚决不允许你打这个存档点去提交的
`01:06` 好那正常来说我们是拦截不住的对吧
`01:09` 你说用户想要去提交
`01:10` 你怎么去拦截他呢
`01:12` 你跟cler code说一声
`01:13` 给我打个存档点
`01:13` 那他就给你打了对吧
`01:15` 所以你要想达到这个拦截的效果呢
`01:17` 我们就可以配置它的
`01:19` 一个叫ho克式的一个工具了
`01:21` 也就是钩子啊
`01:22` 用来去做对应的拦截动作
`01:23` 好
`01:24` 那么这个拦截动作如何去理解呢
`01:26` 我简单画了一下示意图啊
`01:27` 好那我们这里面核心就在于这个拦截
`01:30` 比如说用户去调用一个叫get it save的一个skills
`01:34` 哎这个技能我们在前面呢已经配置过了
`01:37` 也就是当用户去执行这个get save skills的时候
`01:40` 我们的hook就会拦截了
`01:42` 在里面它会拦截一个动作
`01:44` 叫git commit
`01:45` 就是git的一个提交动作
`01:46` 哎他就专门拦截这个动作
`01:48` 他拦截什么呢
`01:50` 他会做检查
`01:51` 检查你是否有两个质检通过的文件
`01:54` 也就是测试通过会形成一个标记文件
`01:57` 质检通过会形成一个标记文件
`01:59` 只有你有这两个文件的情况下
`02:02` 哎我才会放行
`02:03` 让你提交
`02:04` 那如果你不存在唉
`02:06` 我就会要求你进行测试和质检
`02:09` 那大家可能会好奇
`02:10` 那这两个文件来自于什么东西呢
`02:12` 这两个东西
`02:13` 文件来自于我们前面所配置的
`02:16` 质量工程师和测试工程师啊
`02:19` 这两个edit
`02:20` 它在进行质量检查和测试的单元
`02:24` 测试工作之后呢
`02:25` 如果没问题
`02:25` 通过了
`02:26` 就会给我们形成测试
`02:27` 通过和质检通过的标记文件啊
`02:31` 所以说你只有执行了这两个
`02:33` 让让这两个工程师干活
`02:34` 他们干了活才会给你发放这两个通行证
`02:38` 那有了这两个通行证
`02:39` 我们的GCS在被拦截的时候呢
`02:42` 才会检查通过才能放行好
`02:45` 那额外的我们在这个基础上
`02:47` 其实也可以再额外新加一个叫git commit的一个edit
`02:51` 相当于是一个专门用来做题提交的工程师
`02:53` 那这个提交的工程师
`02:55` 他的动作呢就是两步了
`02:56` 如果你要是用这个sub edit
`02:59` 那么他的动作就是标准化动作
`03:01` 它会首先并行
`03:03` 让质量工程师和测试工程师先检查一遍
`03:06` 然后形成标记文件
`03:08` 检查通过
`03:09` 然后再来去调用GCF这个技能
`03:12` 要用这个技能来去做提交
`03:14` 那所以说正常情况下
`03:16` 我们选用这个edit其实就是一个完整的流程
`03:19` 它既会做检查
`03:21` 也会提交
`03:22` 但是如果说用户没有走这个edit
`03:25` 它单独提交就会被拦截住啊
`03:28` 所以这是我们的需求
`03:29` 那同时呢额外的在添加一下哎
`03:32` 如果我们放行提交通过之后的话呢
`03:34` 还需要去删除两个标记文件就可以了
`03:38` 那把那两个背景文件删掉
`03:40` 因为你已经提交了这两个通行证就已经失效了
`03:42` 把它删掉
`03:43` 下一次如果还要再提交
`03:45` 还需要新的测试通过的通行证好
`03:47` 所以这是我们当前的一个需求
`03:49` 那完成这个需求的话呢
`03:51` 我们主体是要做三件事情啊
`03:53` 这里给大家排布一下
`03:54` 我们要做三件事情
`03:55` 第一个第一个我们要添加一个hook
`04:02` 好添加一个hook
`04:03` 这个hook就是我们所谓的钩子啊
`04:05` 可以在可以拦截git的一个叫commit动作
`04:11` 然后检查是否有通行证
`04:17` 啊检查是否有通行证
`04:18` 这是我们的第一个要做的事事情
`04:21` 那第二个要做的事情呢
`04:22` 就是修改两个sub edit
`04:28` 好把这两杀白剂呢改一下
`04:30` 也就是我们的质检工程师和测试工程师
`04:32` 在工作完之后呢
`04:34` 会形成就是工作完后形成叫做通过通过文件啊
`04:42` 标记文件也就我们所谓的通行证啊
`04:44` 他俩在工作完之后
`04:46` 如果没问题
`04:47` 会给你发放两个通行证
`04:48` 在我们的硬盘里面啊
`04:50` 在你的工程里面相当于有两个文件在这里啊
`04:52` 那我们的这个拦截后
`04:54` 可钩子看到通行证才会放行啊
`04:56` 那第三个啊
`04:57` 完了这两个我们再来去再开发一个新的edit啊
`05:02` 这个edit的流程啊
`05:03` 流程就是就是当我们调用这个edit
`05:06` 它就会调用质检工程师以及调用测试工程师好
`05:14` 他就可以正常把这两个工程师调了
`05:16` 你让我们先去跑质检
`05:18` 然后再跑一下测试哎
`05:19` 完事了
`05:20` 最后再来去调用GCF这个技能好
`05:25` 所以我们简单来说要做这三个动作
`05:28` 那三个动作做完之后的话呢
`05:29` 那我们相当于就比较完善了
`05:31` 那不管是说用户他独立调GCF哎
`05:35` 如果你单独调GCF
`05:36` 不好意思
`05:37` 你跑不了
`05:37` 为啥呢
`05:38` 因为你没有去形成这个通行证
`05:41` 所以你无法提交
`05:42` 我会通过配置钩子把你拦截住
`05:45` 那你想要正常调
`05:46` Get it sh5
`05:47` 这个这个技能想让他能够正常用的话
`05:49` 你必须把这个质检工程和测试工程给做了
`05:54` 相当于你要让他俩先干活
`05:55` 然后你才能去把代码呢去提交
`05:58` 去完成我们的存档点好
`06:01` 那那为了省事呢
`06:02` 我们就额外开发一个edit啊
`06:04` 如果说你为了省事
`06:05` 你因为这本账是两件事
`06:07` 你要先让他俩干活
`06:09` 然后再让他干活啊
`06:11` 本账你要执行两步
`06:12` 那为了省事的话
`06:13` 我们把它们都配到一起
`06:14` 我们给你再配一个edit
`06:16` 哎
`06:16` 你只需要调用调用这个edit
`06:18` 它就会自动的让他两个质检和测试干活
`06:22` 然后呢再自动调用get set ve去提交啊
`06:25` 所以我们主体要开发这三个地方好
`06:28` OK那这是我们的简单一个规划
`06:30` 那了解了规划之后的话呢
`06:31` 那我们来回到我们的vs code中好
`06:34` 那我在这里面的话
`06:35` 首先我们把模式的话给它切换成为
`06:36` 这个叫计划模式
`06:38` 那我们先跟我们的cler code呢
`06:40` 现在去聊聊天
`06:41` 去沟通一下
`06:43` 那这个沟通内容很简单
`06:44` 就是把我们刚刚的需求呢去给他描述一下
`06:47` 好我们先回到这边吧
`06:48` 回到这边我们在这边打字
`06:49` 打完字
`06:50` 然后再复制过去好
`06:52` 我跟他沟通
`06:53` 我们说哎我想要实现一个效果啊
`06:57` 想要实现个效果
`06:58` 也就是对于对于git commit
`07:03` 也就是提交动作进行拦截
`07:06` 进行拦截啊
`07:08` 配置一个hook
`07:10` 配了一个hook好
`07:11` 那么怎么拦截呢
`07:13` 拦截后拦截后会检查
`07:15` 拦截后会检查是否通过了
`07:18` 通过了这个叫做单元测试和质量检查啊
`07:25` 是否通过在单车和质量检查
`07:27` 那么通过了放行
`07:29` 通过了放行允许允许存档啊
`07:34` 如果不通过则拒绝好
`07:38` 然后呢如何去验证它的
`07:41` 它是否是通过了呢
`07:42` 啊
`07:43` 如何验证是否通过单次单元测试和质量检查呢
`07:50` 好那么就需要改造
`07:53` 改造与目前叫做目前有的两个edit啊
`08:00` 也就是G即我们的test和质量检查
`08:11` 好记
`08:12` 然后让这两个edit editor在完成工作后
`08:19` 然后呢形成形成叫做测试啊
`08:23` 行程测试就是形成通过的标记文件啊
`08:27` 通过形成通过的标记文件好
`08:30` 那也就是说只有这有这两个标记文件啊
`08:33` 才算是啊
`08:34` 才算是通过了对应的检查啊
`08:36` 所以说这就是我们如何去验证通过
`08:38` 并且呢再配置一个再配置
`08:41` 或者说新建新建一个新建一新建一个edit啊
`08:47` 再新建一个edit
`08:49` 然后名字叫做git commit啊
`08:53` 杠edit啊叫get commit edit
`08:56` 好名字就叫它
`08:57` 然后功能呢功能就是功能就是调用这个edit后啊
`09:03` 调用这个edit后会并行执行tester
`09:11` 和质量检查
`09:18` 啊这会并行执行这个test和质量检查两个edit edit
`09:23` 然后啊然后再调用GCF技能技能进行提交啊
`09:33` 提交好
`09:34` 所以这里面是我们想要实现的
`09:36` 这个动作和效果好
`09:38` 那既然有了这个东西之后
`09:39` 我们再给cloud提个要求
`09:41` 你来看看方案的可行性啊
`09:45` 或者说你来看看我这个想法的可行性
`09:47` java的可行性
`09:49` 然后以及给出行动方案的规划细节
`09:54` 我们讨论一下好
`09:57` 我们先不让他真正执行
`09:59` 像那种像那种复杂的东东西
`10:01` 我们通过开这个计划模式
`10:03` 然后呢跟他好好聊聊天
`10:05` 跟他规划一下哎
`10:06` 看他的回复是什么样的
`10:07` 然后他的回复如果没有问题
`10:09` 哎
`10:09` 那我们就可以去干活了
`10:11` 对吧好
`10:12` 所以说
`10:12` 此时我们现在需要把我们当前提示词
`10:14` 给它粘贴过来
`10:15` 然后呢去直接提交上去
`10:34` 好的
`10:34` 那么经过一段时间等待之后的话呢
`10:37` 他已经帮我们去研究完毕了
`10:39` 然后并且写入了一个规划文档
`10:41` 然后核心的规划文档已经写好
`10:43` 然后核心架构就是我们刚刚所说的
`10:45` 我们的GGCF这个技能会调动这个叫git commit edit
`10:50` 并行跑这个test和这个啊
`10:52` 就是我们的单元测试和质量检查
`10:54` 那质量检查会读这个标记文件
`10:56` 如果都pass才去允许提交好
`10:59` 那这是他给我们所聊描述的核心架构
`11:02` 那跟我们的心里面规划呢
`11:04` 还是略微有一点区别的好
`11:06` 那么等一下我们现在往下面看
`11:08` 他说在征求我们的批准之前
`11:10` 有个点想确认
`11:11` 就是关于拦截范围
`11:12` 他说我们的后可规则只能拦截
`11:14` 可能的内部的叫git commit
`11:16` 也是由cloud去发起的
`11:18` get it提交命令
`11:19` 不能拦截
`11:20` 我们在外部就自己敲的git commit的啊
`11:23` 那这个也是正常的对吧
`11:24` 因为我们如果自己打开终端
`11:26` 我们绕过这个cloud
`11:27` 我们自己去挑敲这个叫git commit
`11:30` 那跟cloud完全就不是一个进程
`11:32` 大家互不干扰
`11:33` 谁也管不了谁
`11:34` 所以这个是正常的
`11:35` 没问题啊
`11:36` 所以我们能够去接受这个限制
`11:38` 好我们回复回复他就是我能接受这个限制
`11:43` 限制
`11:43` 然后呢
`11:44` 你把这个方案啊
`11:46` 把规划给我详细解释一下
`11:53` 啊你先给我详细的解释一下你的我给我的规划
`11:56` 我先看一看到底是不是我想要的这个效果
`12:01` 好了他已经给出来了
`12:02` 我来简单检查一下
`12:03` 看他的一个思路是不是跟我们想要的是一致的
`12:06` 哎我们现在提交代码流程是帮我存档
`12:08` 那直接gi d d commit push
`12:10` 这个没问题
`12:11` 改完之后我说帮我存档
`12:12` 然后他会先派安检员跑这个单元测试
`12:15` 然后去走这个质量检查没问题
`12:18` 然后两个安检员给了pass绿卡
`12:20` 然后才能去gi d d commit push好
`12:22` OK那这是这是我要求他帮我改造后的
`12:25` 然后六个改造步骤来看看
`12:26` 第一个开权限啊
`12:28` 允许读写通行证啊
`12:29` 这个没问题
`12:30` 第二个test iid的这个要改造
`12:32` 因为说哎比如说一般是个通过了
`12:35` 然后呢你就可以去给他写一个通行证好
`12:38` OK那这个没问题
`12:39` 内容是pass或者fail好
`12:40` 这个是他的通行证
`12:41` 那第三个是我们的质量检查工程师的edit
`12:45` 然后他同样会给我们去写一个叫质量结果的
`12:48` 一个test
`12:49` 里面会有一个通行证
`12:51` 然后怎么样算通过
`12:52` 没有高危没有中危啊等等
`12:54` 怎么样算算不通过等等啊
`12:55` 这里面它里面有对应的一个指标
`12:57` 然后再来创建一个git commit agent的
`12:59` 那他是一个安检主管
`13:01` 负责编排整个流程啊
`13:03` 然后等安检干完
`13:05` 然后最多等120秒POSP串断
`13:07` 然后比如说两个标记文件pass都是通过啊
`13:10` 然后呢就可以去提交了啊
`13:11` 这是我们的叫做git commit edit好
`13:14` 第五步改造这个get it save
`13:16` 核心现状是直接AD d commit push
`13:19` 然后改造后现有五个步骤前加了一个步骤零
`13:23` 就是调用我们的刚刚那个叫做质量门
`13:27` 就我们刚刚创建这个edit
`13:28` 也就是说这个技能会直接先调用我们的agent好
`13:32` 然后再来hook会有一个拦截规则
`13:34` 在我们control code里面说帮我get commit会触发拦截提示
`13:38` 你必须走GCF好的
`13:40` 那可以看到他这里面主体上
`13:42` 80%是和我们的需求是相似的啊
`13:44` 但是呢还是有一点细节不一样
`13:46` 比如他这里面需要改造个
`13:48` 这叫这个叫GCF的技能
`13:50` 那我们的规划的这个技能是不会变的啊
`13:52` 我们只是希望就是当你真的commit的时候
`13:55` 给我拦住
`13:56` 你只有通过了我才会让你提交
`13:58` 你不通过我不会让你提交的
`14:01` 但他这里面呢就是他给我们改动
`14:03` 就是在原有的动作之前是先增加了
`14:06` 我们刚刚所说啊
`14:07` 新新创建这个edit
`14:09` 就是它会先调这个edit
`14:11` 那这样的话呢相当于是GCF这个技能啊
`14:14` 他会直接调用这个edit去干活啊
`14:18` 那这个其实就更绕了对吧
`14:19` 我们要保留这个技能的一个叫做完整性
`14:23` 或者说它的独立性
`14:24` 它就是一个git save
`14:25` 他就是单纯的commit push啊
`14:27` 没那么复杂
`14:28` 所以你不要过在这个git chef这个技能里面
`14:31` 还过去调其他的edit
`14:33` 你这绕来绕去绕太多了
`14:35` 第一点
`14:35` 那第二点的话呢
`14:36` 他这个拦截的话会直接拦截啊
`14:39` 这里面我们需要做的就是你别逮着git commit了
`14:42` 就直接拦截
`14:43` 我们要的是你看到gay的commit
`14:46` 你会判断有通行证
`14:48` 你就放行
`14:49` 没通行证你直接拒绝就可以了啊
`14:53` 所以这里面有两点不同啊
`14:54` 我们来告诉他来
`14:56` 这里有两点和我们想象的不同哎
`15:03` 来详细描描述一下第一GCF技能不需要修改啊
`15:10` 保持其业务逻辑的独立性啊
`15:16` 独立性就是单纯的单纯的存档
`15:24` 啊所以你不要不要改它
`15:26` 它就是单纯那个存档技能而已
`15:27` 第二在配置配置hook啊
`15:31` 配置hook做拦截啊
`15:33` 发现git commit后判断是否有通行文件啊
`15:42` 是否是否是否通有通行证啊
`15:44` 通行证即通过了两个检查
`15:50` 两个检查好就发现发现是判断
`15:53` 也发现get it commit后就判断有通行证
`15:56` 如果有通行证
`15:57` 那么则放行啊
`15:59` 允许啊
`16:00` 允许允许提交
`16:03` 那么如果没有通行证
`16:07` 直接拒绝即可啊即可
`16:11` 所以我们只需要啊别的改造都差不多
`16:14` 但是有两点不一样
`16:15` 所以这两个点按照我们想要的这个方式去做
`16:17` 就可以了
`16:18` 然后来我们把这个发给他
`16:20` 让他再重新思考一下
`16:21` 再给我们重新出一下计划
`16:23` 我们来看看他思考后
`16:24` 是否和我们想象的完全一样啊
`16:26` 只有他想的和我们完全一样的情况下
`16:29` 然后我们再去允许他工作
`16:30` 那才能达到我们想要的效果啊
`16:32` 如果他想的和我们不一样
`16:33` 你直接让他工作的话
`16:35` 那肯定的效果就和我们最终最初设想的
`16:38` 肯定是不相同的
`16:39` 好
`16:40` OK那我们再来稍等一下
`16:41` 等他思考一下
`16:44` OK那这里面你看他貌似是明白了我们的想法
`16:48` 但是是否真的明白了
`16:49` 咱也不确定啊
`16:50` 等他跑完之后
`16:52` 我再让他详细的从头到尾
`16:54` 给我解释一下全流程啊
`16:55` 再让他解释的全流程里面
`16:57` 我们检查一下
`16:57` 看看是不是和我们想的一样
`16:59` 哎如果和我们想的一样
`17:00` 那才算是真正的明白了
`17:02` 好的那我们稍等一下
`17:12` 好了他重新去做了一下规划啊
`17:16` 变化1GCF完全不动
`17:18` OK他这个已经删掉了这个念头啊
`17:20` 这继续用它做单纯的存档
`17:22` OK没问题
`17:23` 第二个hook变成真正的闸机
`17:25` 之前hook拦一下
`17:26` 然后提示用git save实际的质量检查
`17:29` 在get set里面做
`17:30` 我们要求不允许改这个技能
`17:31` 那所以他就不得不让做了
`17:32` 现在变成一个通行证检查器啊
`17:35` 命令触发了会会检查运行这个脚本啊
`17:38` 读两个脚本的第一行
`17:40` 如果两个pass返回零
`17:41` 然后OK没问题
`17:42` 然后如果去
`17:42` 如果是不是pass
`17:44` 然后就被拒绝
`17:45` OK这个没问题
`17:46` 然后通行证从哪里来
`17:47` 通行证哪里
`17:48` 从我们这个edit来
`17:49` 他会先跑这个test
`17:50` 再加这个叫quality
`17:52` 好把那两个跑了
`17:53` 两个都pass
`17:53` 发了通行证
`17:54` 然后才能commit的好
`17:56` 然后完整的流程帮我提交代码
`17:58` 然后调用这个git commit edit
`18:01` 然后清理旧通行证
`18:03` 然后跑测试
`18:04` 然后审查没问题
`18:05` 读通行证pass触发
`18:06` 然后个毒发没问题
`18:08` 被拦截情况
`18:09` 我说git commit偷偷提交
`18:10` 然后找不到通行证就给我们返回好的
`18:13` 那通过这个描述
`18:14` 我们会发现哎
`18:15` 他整体上的想法
`18:17` 就跟我们的设计就基本上差不多了啊
`18:20` 我们原本有一个叫做提交之后呢
`18:23` 是需要把旧的通信证给他删掉
`18:25` 那他现在是帮我们给他设计到了
`18:28` 就是在我们跑测试之前
`18:30` 可以把旧的通行证先给它清理掉
`18:33` 哎那这个也是可以的
`18:34` 没问题
`18:35` 反正最终就是我们这些旧通行证都需要清掉
`18:38` 不管是提交后清掉
`18:39` 还是说下一次跑之前去给它清掉都是可以的
`18:43` 好的我们整体看了一下啊
`18:45` 他这个流程和我们设想的是基本差不多
`18:47` 99%啦
`18:48` 那就稍有一点点不影响
`18:50` 就是关于旧的通行证
`18:51` 这里啊我们忘了跟他说了
`18:53` 就是说我们有个旧通行证需要清理的动作
`18:55` 他他其实是想到了
`18:57` 但是呢他的动作流程不太好
`18:59` 它是在我们调用这个id的时候
`19:02` 再去清理这个旧的通行证啊
`19:05` 那这个其实就有点晚了对吧
`19:06` 你只要能够成功的就pass放行提交git push成功后
`19:12` 就把旧通行证给我清掉就可以啊
`19:14` 这样避免什么
`19:16` 避免你旧的通行证没有被清清掉
`19:18` 然后人家不断的偷偷摸摸提交了
`19:20` 那肯定不合适
`19:21` 只要成功一次
`19:22` 通行证立马就清掉
`19:24` 下一次想要再提交
`19:26` 必须发放新的通行证
`19:27` 这样会更加安全一点
`19:28` 所以我们说啊整体上OK啊
`19:30` 整体OK这就是有一个小细节需要改动
`19:38` 啊有些小细节需要改动
`19:40` 就是我们的我们当当git push成功后啊
`19:46` 立刻删除旧的通行证即可
`19:52` 好把立刻删除旧的通行证
`19:54` 我们把这个需求给他描述一下
`19:56` 让他重新再生成一下规划文档
`19:58` 然后我们最后再来去检查一下
`20:00` 如果确定没问题
`20:01` 就可以让他真正干活了
`20:02` 你看这里面他也说了
`20:03` 合理啊
`20:04` 通行证是一次性的
`20:05` 用完就销毁啊
`20:07` 防止下一次拿通行证否蒙混过关啊
`20:10` 所以很多时候你得自己思考一下
`20:12` 否则的话AA有时候考虑他也不是那么严谨
`20:14` 对吧啊
`20:15` 这也再正常
`20:16` 要不然的话你看我们前面写的代码
`20:18` 让AI写的代码
`20:19` 然后我们就让AI去做质量检查
`20:21` 照样能检查出来一堆问题啊
`20:23` 这是因为啊AI在写的时候呢
`20:25` 它只是满足你的功能而已
`20:26` 你并没有给他提出来详细的要求
`20:29` 那除非你跟他说哎把代码给我写的超级规范
`20:32` 给我写的超级严谨
`20:33` 各种各样的安全机制都不能少
`20:35` 哎除非你在让他实现功能功能的时候
`20:37` 你就给他这样说了啊
`20:38` 那否则的话呢他就是按照正常的方式给你写
`20:41` 然后续你再检查
`20:42` 你会发现唉它可能功能没问题
`20:44` 但是可能但是会缺少一些安全机制呀
`20:46` 或者等等的东西啊
`20:47` 所以说我们这边也一样
`20:49` 你跟他聊规划的时候呢
`20:50` 他很多人说他是规划是大体上没问题的
`20:52` 但有些细节你还是要自己看一下
`20:54` 如果你觉得他有哪些哪些不对
`20:56` 你跟他说一下
`20:57` 那其实他也能够去理解
`20:58` 然后去变得更好好的
`21:00` 那这里面你看他现在问
`21:01` 我们在当前这个规划里面就出来了啊
`21:05` 这里面你看他把整个规划里面的步骤五
`21:07` 这里面的一些进行一进行了修改
`21:09` 比如说这里面的添加了叫做步骤期
`21:12` 立刻销毁通行证啊
`21:14` 防止下一次蒙会过关的一个动作好
`21:17` 然后更新整个架构图好
`21:19` 然后这里面规划更新改动很小
`21:20` Ok
`21:21` 那整体来说就把这个改动给我们搞定就可以了
`21:23` 然后没问题的话就可以去接受这个计划
`21:26` 好点击我接受这个计划
`21:28` 或者说你点击yes
`21:30` 点击第二个的话
`21:31` 表示的是我只接受这个修改啊
`21:34` 这个整体计划每次你还要问我啊
`21:36` 但是正常情况下
`21:37` 我们其实跟他们聊的差不多了啊
`21:39` 整体上可以接收这个计划了
`21:40` 并且呢应该这叫auto accept
`21:42` 就自动自动同意啊
`21:44` 没问题
`21:44` 我觉得基本上来说
`21:45` 后面就让他去自己去干活就行了
`21:47` 好那我们来接受这个计划
`21:49` 来好
`21:50` 接受计划之后的话
`21:51` 他现在就可以来去干活了
`21:52` 哎计划搞定
`21:54` 开始实施
`21:55` 按照我们的规划
`21:56` 他去去编写我们的hook
`21:58` 那这个hook的话其实是写的这个叫setting试点
`22:00` JASON里面
`22:01` 也就是在我们当前这个叫cloud点cloud文件夹里面
`22:04` 应该有个叫setting试点
`22:06` JASON啊
`22:06` 就这个啊
`22:07` 我们的hook是编写到这里面的
`22:09` 那他的写法呢是比较专业的
`22:10` 对于我们小白来说可能不会写
`22:12` 所以没关系
`22:13` 我们就不写了
`22:14` 但是我们把规划给他做好
`22:16` 计划做好
`22:16` 让cloud帮我们写就行好
`22:18` 这里面他需要去编辑这个叫CDC点JSON哎
`22:22` 就是我们所说的这个文件啊
`22:23` 这里面去添加后可来yes
`22:25` 允许一下
`22:28` 好了
`22:28` 然后让他去继续思考
`22:30` 然后他就问我们是否继续编辑
`22:31` 那我们来yes
`22:32` 来继续编辑
`22:33` 继续允许
`22:36` 好我们通过这个小窗口
`22:37` 其实可以看到
`22:38` 它里面其实已经帮我们这个hook给写好了
`22:40` 这个叫PRE叫pour to use
`22:42` 叫做叫做工具使用之前对吧
`22:45` 在工具调用前
`22:47` 然后被洗
`22:48` 你看这里面是不是会有我们的
`22:49` 比如叫git commit的一些相关的命令的拦截啊
`22:52` 等会等他写完之后
`22:53` 我们再去具体看一下
`22:54` 或者在这里面可以看到
`22:55` 他其实已经给我们去做了一下
`22:57` 关于这个叫git commit
`22:59` 一个基础的拦截啊
`23:00` 是一个啊
`23:01` 是一个叫做hook啊
`23:02` Hook
`23:03` 等会我们再来看来
`23:04` 它里面又要去编辑
`23:05` 来点击这个yes
`23:07` 好
`23:07` 可以看到他现在已经是完成了
`23:09` 这个叫setting试点JSON啊
`23:11` 下面要去执行别的命令
`23:12` 我们来去允许他执行这个命令
`23:13` 好在他执行的过程中呢
`23:14` 我们可以来看一看当前这个叫setting试点JASON啊
`23:17` 那这个setting试点JSON里面
`23:19` 其实就是我们当前整个工程里面
`23:20` 跟可乐的一些沟通啊
`23:22` 一些允许啊
`23:22` 一些放行的机制啊
`23:24` 这里面需要添加一个脚本
`23:25` Yes
`23:25` 好
`23:26` 那么回过来
`23:27` 你看这里面都是我们的一些叫做allow
`23:29` 允许的一些命令啊
`23:30` allow允许的
`23:31` 我们这里面啊
`23:32` 拜写npm inst12
`23:33` NPMCONFIG都是我们之前点的允许的啊
`23:36` 他说他能记住
`23:37` 然后续的话就不会再询问你了
`23:38` 好了
`23:39` 你看下面这个就是hook
`23:40` 这个hook
`23:40` 就是我们刚刚所说的一个叫做叫做拦截器啊
`23:44` 拦截器啊
`23:45` 这里面他要去添加这个MD来夜视来好
`23:47` 我们回过来再来看啊
`23:48` 当前的hook
`23:49` 你看它里面就是针对了一个叫if就是判断啊
`23:52` 然后判断谁呢
`23:53` 判断是git commit相关的东西
`23:55` 说白了就是当前它呢
`23:57` 就是对我们刚刚所说的叫git commit啊
`24:00` 也就是get it commit去进行了一个拦截
`24:02` 那拦截动作是啥呢
`24:04` 它会执行一个叫脚本的
`24:05` 叫做check quality
`24:06` get叫做质量门点SH脚本啊
`24:09` 他会帮我们生成这个脚本
`24:11` 由这个脚本来去对我们当前的这个git提交
`24:14` 取得判断
`24:15` 就是判断你当前这个通行证
`24:17` 到底有没有没有下发啊
`24:18` 所以说他这个hook呢就已经配置好了
`24:20` 是一个拦截器啊
`24:21` 拦截器好
`24:22` 然后他现在的话呢又需要去添加我们对应的
`24:25` 一个叫做质量工程师的一个修改啊
`24:28` 他要把我们整个质量工程师去改一改
`24:30` 其实就是我们要让他去增加所谓的
`24:32` 发放通行证的一个功能啊
`24:34` 发放通行证的功能啊
`24:36` 所以对于我们来说给他一个yes啊
`24:38` 允许就可以了
`24:38` 等会我们最后再来去看一下来
`24:42` 好可以看到这里面
`24:43` 这里面其实也能看到它里面
`24:44` 你看有关于这个叫写入通行证的一些规则啊
`24:47` 好然后下面这是这也是又一个编辑
`24:50` 也是我们的质量工程师的MD
`24:52` 好
`24:53` 这里面你看它里面有一些修改
`24:54` 比如说关于这个叫做默认只读呀
`24:57` 还有像质量门模式
`24:58` 这就是我们新增的质量门模式对吧
`25:00` 它需要有一个要通过或失败的判定标准
`25:03` 然后去然后呢去往那个文件里面去写入pass啊
`25:07` 写入这个通通过的一个通行证啊
`25:09` 就是我们所谓的通行证
`25:10` 这个是质量工程师他的一个描述文件
`25:13` 好
`25:13` 我们来给个yes
`25:14` 允许来修改好
`25:17` 那现在开始来到第五步了
`25:18` 就是创建我们所谓的一个新的edit
`25:21` 就专门做这个git commit的
`25:22` 那这个新的id呢
`25:23` 你可以看到它的内容呢
`25:25` 就是去做git提交的一个质量守门员啊
`25:28` 他的动作流程
`25:29` 首先呢清理旧的通行证
`25:30` 这个没问题
`25:31` 然后确保目录存在
`25:32` 这个也没问题
`25:33` 然后并行启动两个检查id的
`25:36` 也就是单元测试检查和质量检查
`25:38` 哎
`25:38` 这里面就是并行让这两个id的跑起来
`25:40` 完事之后的话呢
`25:42` 就等待他俩去去去完成
`25:44` 然后呢去读取并判定结果
`25:46` 去读取他们他们是否发放的通行证啊
`25:49` 如果整体通过
`25:50` 就可以去执行git的一个提交动作了啊
`25:53` 如果说如果说提交通过
`25:55` 就会立刻把通行证再给他销毁掉
`25:57` 哎
`25:57` 你看这里面
`25:58` 就是满足了我们所需要的一个需求了
`26:00` 好的那这样没问题
`26:02` 我们得点击yes啊
`26:03` 允许它编辑好的
`26:06` 那目前为止
`26:07` 他所有的工具呢都已经去配置完成了
`26:10` 现在他走到那个叫验证阶段
`26:11` 叫运行测试啊
`26:12` 查看一下后壳的一个配置啊
`26:14` 我们等他验证一下
`26:17` 好的OK那目前为止他已经是完成了啊
`26:21` 完成了之后的话呢
`26:22` 他说需要重启才能让后壳生效
`26:24` OK那我们等会给他重启一下
`26:26` 但在重启之前
`26:27` 我们可以来看看它所写的这些东西
`26:29` 比如我们要加了一个新的EDINES
`26:31` 已经给我们加进来了
`26:32` 就是这个叫git commit的
`26:33` 刚刚给大家去看了
`26:34` 清理旧通行证
`26:35` 确保目录存在启动两个并行检查的edit好
`26:38` 然后再来看看我们原本老的那两个edit
`26:40` 这两个老的老的edit里面
`26:42` 我们来找一找它
`26:43` 应该正常来说
`26:44` 应该会在最后给我们去添加一个关于安全
`26:48` 安全的一个规则
`26:50` 来找一下质量门
`26:51` OK在这里啊
`26:52` 如果添加质量门模式啊
`26:53` 会确保会写入这个叫通行证的文件啊
`26:56` 这是我们质量工程师
`26:57` 还有我们的测试工程师
`26:59` 在下面应该也会有写入通行证文件的一个描述
`27:02` OK那他的这个修改呢也是完成了
`27:04` OK那这样的话我们简单验证一下
`27:06` 大体上没问题
`27:07` 我们就可以把这个vs code给他关闭了
`27:09` 然后重新启动试一下来
`27:14` 好的
`27:15` Ok
`27:16` 那现在的话呢
`27:16` 我们是已经把这个叫visual studio给他重启了
`27:19` 那重启维修studio
`27:20` 相当于是重新进入了我们的可road code好
`27:23` 那我们再来去看一下
`27:24` 你看我们的通行证是在这个叫点cloud
`27:27` 点水SD10点里面有个叫test点啊
`27:30` result点TIT的
`27:31` 那我们来简单看一下
`27:32` 我们当前这个叫点cloud
`27:33` 里面是不是没有这个叫result4的一个文件呀
`27:36` 那说明我现在是没有通行证的
`27:37` 那没有通行证
`27:38` 我们来试一下
`27:39` 来我们来偷偷摸摸提交一下
`27:41` 来帮我帮我帮我将代码啊
`27:47` 就是说直接执行git git命令啊
`27:50` 命令啊
`27:51` 提交代码啊
`27:53` 提交代码啊
`27:54` 直接执行get的命令
`27:55` 提交代码
`27:55` 我我来做一下这个要求
`27:56` 看他能否给我们触发拦截
`27:58` 又或者说自动调用我们刚刚所写的edit啊
`28:01` 不管是哪种方式都是可以的
`28:02` 你要调到edit
`28:03` 那它会自动帮我们去跑质检和单测
`28:06` 那你要是不调用那个EDIN
`28:08` 你直接偷偷摸摸跑
`28:10` 我们现在是没有通行证的
`28:11` 你也是提交不了的啊
`28:12` 所以两种方式看他走哪一种
`28:14` 我们都能去验证到效果
`28:18` 好的可以看到
`28:19` 其实我们现在是已经给他派发了这个ending的
`28:22` 所以说正常来说
`28:24` 你只要跟我们的可乐的去沟通
`28:26` 你是无法绕过的
`28:27` 哎我们这个AE呢就会帮你去走这个质量检查
`28:30` 和我们对应的这个啊
`28:31` 和对应的这个叫单元测试啊
`28:33` 所以说你看提交流程
`28:34` 派交给了这个I进的去处理好
`28:36` 那我们现在比如说先先给他这个取消了
`28:39` 我们按按ESC啊
`28:41` 先把整个流程给他取消了
`28:42` 我们可以明确告诉他
`28:43` 哎
`28:43` 比如说不允许不允许派派发啊
`28:48` 派git commit杠edit去处理啊
`28:52` 你直接执行git命令提交代码啊
`28:56` 我们现在说你别给我走edit了
`28:58` 你就单独的给我跑一下
`29:00` Get it
`29:00` 我看你能不能给我拦住对吧
`29:03` 我就单独执执行你
`29:07` 好
`29:08` 可以看到我们现在这个bench
`29:09` 里面有个明显的提示
`29:10` 叫做pro to use
`29:12` 叫base hook error啊
`29:13` 你看hook触发了啊
`29:15` hook触发了导致脚本执行失败啊
`29:17` 修复一下好
`29:18` 那这个其实里面说白了还不是我们呃
`29:21` 这个hook确实是执行了
`29:22` 但是呢这个脚本也有点问题
`29:25` 还没还没有到那啊
`29:26` 安全检查这一步好
`29:27` 没关系
`29:28` 我们让他把脚本简单修复一下
`29:29` 再重跑一下就可以了
`29:32` 好可以看到我们的hook呢又再一次从事提交
`29:35` 然后呢又又触发了这个hook
`29:37` 然后hook呢去进行了一个检查啊
`29:40` 然后他这里面需要去执行
`29:42` 我们当前这个叫检查脚本啊
`29:43` 我们就允许他去执行这个脚本就行了啊
`29:46` 然后这里面你看叫质量门未通过
`29:48` 缺少检查
`29:49` 结果是直接给我们锯掉了啊
`29:51` 那这样说明我们当前这个后和拦截呢
`29:53` 是没有问题的
`29:54` 对吧啊
`29:55` 后和拦截没问题好
`29:56` 你看他说脚本本身是好的
`29:59` 然后呢后可执行环境找不到被写动满减动作
`30:01` C4好
`30:02` 那这里面他又去进行了一个脚本的修改
`30:04` 好我们再来给他个yes
`30:06` 让他再跑一下
`30:14` 好了
`30:15` OK那么经过一段时间等待之后的话呢
`30:17` 其实我们会发现我们的拦截呢
`30:19` 其实是屡次失败了
`30:20` 这个失败其实并不是在于我们的hook脚本
`30:24` 而不是说不不是在于我们hook本身的问题
`30:26` 而是在于我们当前的操作系统环境
`30:30` 因为其实本质上来说啊
`30:31` 像我们这些可录的code啊
`30:32` 像那些编程工具
`30:33` 本质上在LINUX环境下
`30:35` 或者说Mac系统环境下是最为常见的
`30:38` 因为它很多的命令都是LINUX
`30:39` 像拜洗命令啊等等
`30:41` 所以你看我们当前的问题是在在windows系统下去
`30:44` 通过命令行去执行那个叫BS脚本
`30:46` 其实本质很很多时候屡次失败啊
`30:48` 说白了就是他没有这个叫做脚本的执行环境
`30:51` 然后最后执行power shell版本
`30:53` 然后commit的放行了
`30:55` 就说明他其实本质来说
`30:56` 是帮我们把这个存档点搞定了啊
`30:59` 然后他里面也告诉我说
`31:00` BS环境可能在windows下不兼容power shell脚本的话
`31:03` 可能转译有可能有问题
`31:05` 那我们现在他说建议建议下次重启后
`31:08` 再来去试一下
`31:09` 然后再来看看这个CONNEI是否能够正确拦截好
`31:12` 那我们在它重启之前
`31:14` 我们要求他就是你认真检查一下啊
`31:18` 认真检查一下
`31:22` 检查一下
`31:24` 检查一下这个脚本内容啊
`31:26` 脚本内容啊
`31:27` 然后确认确认可以跑
`31:30` 我再我再重启啊
`31:33` 你先给我检查一下别人
`31:34` 免得我重启之后你又不行对吧
`31:36` 我们在真正重启之前
`31:37` 让他自己第二次对自己的内容去做一下确认啊
`31:42` 他会再次检查一下的啊
`31:43` 看看他检查结果如何啊
`31:45` 那这个我们稍稍等待一下
`31:46` 因为对于我们这些调试的话很正常啊
`31:48` 很正常
`31:49` 这里面的很多调试的东西呢
`31:51` 都需要去慢慢的让AI模型去一点点去修复啊
`31:54` 等等啊
`31:55` 这里面你看他对我们当前这个脚本呢
`31:57` 去进行了个修改啊
`31:58` 你看到他说这个问题明显
`32:00` JASON里面嵌套转移是个灾难
`32:02` 读改动独立的PS1脚本干干净净
`32:04` 你看你这里面单独给他说一声
`32:07` 让他独立检查一下
`32:08` 就会修复一些问题啊
`32:10` 所以我们来yes
`32:10` 允许一下好
`32:13` 然后这里面他对于我们当前这个setting
`32:16` 里面的命令啊
`32:16` 从刚刚的这个一大堆就给他改成了什么
`32:19` 改成了我们的power sh去执行windows内置的这个脚本好
`32:22` 我们来给他个yes啊
`32:23` 允许他这个修改
`32:25` 然后现全面验证脚本的三种场景
`32:27` 无通行证应该拦截
`32:28` yes等等
`32:29` 我们给它运行一下好
`32:31` 让他去帮我们验证一下
`32:35` 好OK他里面又发现问题了
`32:37` 路径里面含中文
`32:38` 那他这里面需要去改成一些啊
`32:40` 其他编码等等
`32:41` 那我们来给它个yes
`32:54` 好的OK那么经过他一段时间的验证之后的话呢
`32:58` 其实是把我们的脚本进行了大改啊
`33:00` 那这个过程里面其实他已经又把这个power shell
`33:03` 就是windows里面的叫做power shell脚本删掉了
`33:06` 改改成了叫agent
`33:08` 一个是我们的大模型去判断
`33:10` 那其实这也算是一个好方式啊
`33:11` 因为很多时候的话呢
`33:12` 你用脚本都算是固定的命令啊
`33:15` 固定的编程代码
`33:16` 但你要用这种模型去判断
`33:18` 相当于把文件告诉他
`33:19` 然后你告诉我这里面内容是通过还是不通过
`33:22` 由模型那个脑袋去思考
`33:24` 那这样的话也会更加的兼容一些
`33:26` 因为确实啊我们的windows环境在命令行
`33:29` 在编写脚本这一块跟LINUX还是差的太远太远了
`33:33` 那如果是LINUX环境下的话
`33:34` 又或者说在Mac环境下
`33:36` 其实我们这个动作是非常非常简单的
`33:38` 但是由于是在windows环境下
`33:39` windows这个命令行实在是说难听点还是比较恶心的
`33:43` 所以很多时候这个AI发现在windows环境下
`33:46` 他直接就把这个命令行的方式给你pass掉了
`33:48` 直接给你走这个模型的脑袋去思考好
`33:51` 那所以可以看到我们在这个setting里面啊
`33:54` 可以简单看一下它这个叫hook
`33:55` 就从这个叫command的命令行给它变成叫edit
`33:58` 然后里面有个PROMP
`33:59` 它会告诉你你是一个git提交的一个提交门啊
`34:03` 然后呢
`34:03` 去使用你的模型
`34:04` 去判断它里面到底是不是有一些pass的一些标记
`34:07` 对吧
`34:08` 文档是哪哪几个文档
`34:09` 然后这里面会告诉你有有哪有哪些
`34:11` 是不是有那个叫pass的标记等等啊
`34:14` 这里面我们就不去仔细读了
`34:15` 它大体上就这个意思
`34:17` 通过这个提示词来去让模型去思考
`34:19` 告诉我们通过还是不通过啊
`34:24` 好的
`34:24` 那这里面他说说我们重启后即可生效啊
`34:28` 然后我们再说你再次检查一下
`34:30` 再次思考检查一下
`34:34` 检查一下啊
`34:35` 没问题
`34:36` 我就重启
`34:39` 好我们还是让他再思考
`34:41` 确认一下来
`34:42` 没问题
`34:42` 我再重启吧
`34:43` 啊这样的话看看有没有什么隐藏性的问题啊
`35:02` 好的OK他又经过了一次检查啊
`35:05` 然后大体上没问题
`35:07` 稍微修复了一点点的小东西啊
`35:09` 这个没啥关系
`35:10` 那我们现在检查通过
`35:11` 他告诉我们可以重启的
`35:12` 那我们就要重启它吧
`35:14` 来我们把这个viso studio给它关闭了
`35:16` 好关闭之后呢
`35:17` 再来去打开这个viso studio code
`35:22` 好OK那重启了之后的话呢
`35:24` 我们现在的话就相当于是来去测试一下啊
`35:27` 首先先回到我们的绘画啊
`35:28` 或者说我们就来开一个新的绘画吧
`35:30` 来好
`35:31` 然后在这边的话我们随便去添加一些新的东西
`35:34` 比如说我们添加一个新的文件啊
`35:37` 新的文件就叫做啊mi点TST好
`35:41` 我们在整个这个工程里面加个新文件
`35:44` 然后我们说这是这是这是这是测试文件啊
`35:48` 是测试文件啊
`35:50` 文件文件用来测试git hook啊
`35:55` hook的好
`35:56` 我们随便加个文件
`35:56` 随便打点字
`35:57` 反正相当于我代码有了修改
`35:59` 那既然有了修改
`36:00` 我就可以去把整个代码呢去提交啊
`36:02` 然后来看看他是不是通
`36:04` 是不是能帮我们拦截下来
`36:05` OK那我们现在来开始给他给要求
`36:07` 就说啊不要调用杠git commit
`36:11` 杠agent代理
`36:12` 然后直接代理啊这个旁边的这个代理
`36:18` 然后直接执行执行git命令啊
`36:23` 帮我提交
`36:25` OK我们再来测试一下啊
`36:27` 让他直接给我跑命令
`36:28` 然后给我提交
`36:29` 看能不能触发这个hook
`36:34` 好的
`36:34` 可以发现他其实还是没有去触发我们的hook
`36:36` 那么我们来问他一下
`36:38` 帮我验证
`36:39` 帮我检查一下
`36:41` 为何我这个提交动作没有触发
`36:45` 没有触发后可检查
`36:52` 好他是说我们没有配置git的hook
`36:54` OK那我们其实本质来说
`36:56` 我们配置的不是git的hook
`36:57` 我们配置的是可LOD的hook
`36:59` 我们来告诉他就是我配置的是cloud code的hook啊
`37:05` Hook
`37:16` OK那么经过检查呢
`37:19` 他其实已经找到了原因了
`37:20` 就是我们原本配置的这个脚本
`37:22` 里面的那个叫配置规则啊
`37:24` 来找到这个叫setting4啊啊
`37:26` 这个身体是JSON里面
`37:27` 我们配置拦截规则呢是叫做if
`37:29` 就是如果是B洗命令叫git commit相关的啊
`37:33` 那它表示的是git commit的开头才能匹配到
`37:36` 但由于他给我们真正提交的命令是什么
`37:39` 他提交的命令是一个组合命令
`37:41` 叫get it i d d
`37:42` 然后get commit等等一系列组合
`37:44` 它不是以git commit开头
`37:46` 而是以get it i d d开头
`37:47` 所以说我们后可呢其实并没有去匹配到
`37:49` 因为他不不满足条件对吧
`37:51` 我们要找git commit的
`37:52` 但实际他是get it i d d
`37:54` 所以他现在现在
`37:55` 现在给了我们一个简单的操作啊
`37:57` 简单操作就是他需要我们把这个叫做拜洗命令
`38:01` 给他改成那个叫星点git commit的星
`38:04` 也就说明就是通过通配符来去确保
`38:07` 只要包含了git commit的命令就都OK好
`38:10` 然后同时还有第二个原因
`38:11` 就是两个检查文件的结果都是pass啊
`38:14` 都是pass
`38:14` 也就说我们这边你看有个叫result
`38:16` 是有两个检查结果
`38:17` OK那我们把这里给他直接删了啊
`38:19` 直接删除了
`38:20` 或者我们我们不删
`38:21` 我们就跟他说一下
`38:22` 我们就说OK就是帮我改吧
`38:24` 帮我改改改一下
`38:26` 然后呢同时删除两个检查pass的文件
`38:33` OK你看他我要让他们帮我改一下
`38:35` 其实本质来说就是星
`38:36` 然后git commit
`38:37` 星也是只要整个命令里面包含get commit
`38:41` 都会触发我们的hook的啊
`38:43` 这样的话应该是更加稳妥一些
`38:45` 来点击yes
`38:45` 允许修改啊
`38:46` 并且呢他要去执行这个删除
`38:48` 我们来允许一下来yes
`38:50` 把这两个文件给我删掉好了
`38:53` 完成了完成之后的话呢
`38:54` 那我们比如说再来去修改一下
`38:56` 我们说啊拉拉拉拉拉拉拉啊
`38:58` 随便改点东西
`38:59` 现在这个整个代码工程是不是又有了
`39:02` 又有了更改呀
`39:03` 啊你可以看到左边那个有个一
`39:04` 就代表当前这个当前我们的这个代码
`39:07` 跟我们的存档点是有一个文件的差异的
`39:10` 这个差异就是叫my text
`39:12` 跟我们上一个存档点的差异在于啊
`39:15` 他多了这一条啊
`39:16` 所以通过这个一
`39:17` 你会发现我们当前这个整个工程呢
`39:19` 肯定是有所修改的啊
`39:20` 那我们现在就来跟大家说啊
`39:21` 继续继续
`39:22` 我们找到向上翻向上翻啊
`39:24` 不要调用
`39:25` Get it
`39:25` get it commit id的代理
`39:27` 直接git命令帮我提交来走
`39:29` 你
`39:35` OK可以看到当前我们当前这个后
`39:37` 可能是已经啊拦截了
`39:39` 然后这个命令被拦截
`39:41` 然后质量门判定不通过
`39:42` 阻止了提交
`39:43` OK那可以看到我们现在的话呢是已经把他的
`39:47` 把他那个提交给阻止掉了
`39:49` OK那现在的话我们验证了这个叫git的一个啊
`39:53` 不是验证这个hook的拦截的动作没问题了
`39:56` 那hook拦截动作没问题的话
`39:57` 我们来去跑一下我们的智能体
`39:59` 也就是说我来帮我提交代码
`40:03` 啊允许允许edit啊
`40:06` 允许agent
`40:08` 因为我们在绘画里面明确的指定过
`40:11` 我们不允许跑ANITT
`40:12` 所以说你如果单独的说帮我提交代码
`40:15` 他可能还会认为你不允许I进的执行
`40:18` 所以我们后面额外说一句
`40:20` 我们说允许edit去执行啊
`40:22` 这样的话看他能不能帮我们去跑edit
`40:29` 好了
`40:30` 他现在是要去删除这个叫旧的这个通行证
`40:33` 来允许啊
`40:34` 这是我们当前这个agent其实已经跑起来了
`40:36` 你可以看到这里面已经是走了这个EDIN
`40:38` 然这边有一个叫做思考过程
`40:41` 你看这里面告诉我们去跑
`40:42` 这叫git commit edit啊
`40:44` 通过这个edit去跑起来
`40:46` 所以现在已经走到我们这个
`40:48` 走到我们这个edit的一个内内容了
`40:50` 那在这个edit里面
`40:51` 其实我们配置过第一步
`40:52` 是不是就是清理这个旧的通行证
`40:53` 所以他刚刚的命令
`40:55` 就是询问我们是否允许执行这条命令
`40:57` 好OK然后现在又是第二条命令啊
`40:59` 他要去创建文件夹
`41:01` 去创建这个啊
`41:02` 叫做看一下
`41:10` 好
`41:10` 他要去创建我们整个这个叫做安全检查的
`41:13` 一些相关的目录啊
`41:14` 好
`41:15` 然后以及再来
`41:16` 是否允许去写这个叫做安全检查的文件来yes
`41:19` 视允许把它写进来的话
`41:21` 看看他写的是什么
`41:22` 我给他写了个pass
`41:23` 那就说明当前这个叫做单元测试呢
`41:25` 是没问题的啊
`41:27` 单元测试没问题
`41:27` 那我们稍等一下
`41:28` 再来等它生成第二个通行证
`41:30` 第二个通行证是来自于我们的叫质量检查
`41:34` 看质量检查是否能够我们生成通行证
`41:47` 好的
`41:48` 那么经过一段时间的思考
`41:50` 那么他已经把质量检查跑完了
`41:52` 但是实际上我们看了下质量检查的效
`41:54` 这个结果啊
`41:55` 其实本质来说是FA啊
`41:57` 也是失败了啊
`41:58` 然后他这里面告诉我们
`41:59` 我们的注释的话呢没有达到阈值20%
`42:02` 其实本质来说安全性问题是没有的
`42:04` 没有高危
`42:04` 也没有中温中微
`42:06` 然后代码质量也都是完全OK的
`42:08` 就唯独就是在注释这里
`42:10` 没有达到我们所需要的一个阈值啊
`42:12` 所以说
`42:13` 所以说我们当前这个质量检查是不通过的啊
`42:17` 不通过的
`42:18` 然后他现在告诉我们
`42:19` 质量门检查覆盖率被挡
`42:21` 被挡住了
`42:22` 门槛的20%
`42:23` 所以他要给三个文件补充注释
`42:25` 然后再提交
`42:25` OK这个也没问题
`42:26` 我们允许你补充注释
`42:28` 然后再重新试一下
`42:29` 让这个质量检查能够第二次能够通过好
`42:32` 我该让他继续跑一下
`42:36` 啊这是他的思考过程
`42:37` 我们可以给他啊
`42:38` 给他关闭一下啊
`42:40` 这样的话省得他啊P的话出来一大堆的东西
`42:43` 我们只看核心点就行了啊
`42:47` 这里面你看他为了满足这个
`42:48` 20%的一个注释率啊
`42:50` 你看给我们加了不少的注释啊
`42:59` OK他现在注释已经完成了一个叫API
`43:02` 点ts文件的一个补充啊
`43:04` 这现在正在为第二个文件补充注释
`43:06` 以及后面有有三个文件都要去补充注释啊
`43:09` 三个文件的注释补充完的话
`43:11` 就会重新运行我们的质量门去检查一下啊
`43:14` 如果检查没问题
`43:15` 就会再重新提交代码
`43:16` 我们让他继续跑一下来
`43:21` 好你会发现
`43:22` 其实有的时候AA也会有这种叫做为了加注释啊
`43:25` 加注释
`43:25` 你看这里面其实注释已经很完善了
`43:27` 但是他相当于给他加加加正数对吧
`43:29` 哎一个舌头
`43:30` 现在给你变成舌头亮绿色发光效果啊等等啊
`43:33` 说白了
`43:33` 其实他也是为了满足我们20%的阈值
`43:35` 那当然了
`43:35` 其实我们到时候可以去改一下
`43:37` 比如我们这个质量检查里面可以搜一下
`43:39` 应该有个20%的一个啊
`43:41` 这个字样你看到20%对吧啊
`43:43` 可以后续我们这一次就按20%跑
`43:46` 后续的话你可以稍微改一下
`43:47` 你把它阈值改为十就可以了啊
`43:50` 不需要那么高对吧
`43:51` 不需要那么高
`43:52` 那么这样的话他就可以去通过对吧
`43:54` 通过通过的可能性就会更高一点啊
`43:56` 因为我们这种安全性的问题
`43:59` 或者代码质量的问题
`44:00` 我们都可以让你去
`44:02` 这都可以让你不通过
`44:04` 但是注释说白了
`44:05` 因为注释不通过
`44:07` 是不是就有点坑呀啊有点有点有点尴尬啊
`44:10` 所以说这个注释可以把他阈值放低一点啊
`44:12` 放低一点
`44:14` OK但现在他已经开始去补充注释了
`44:16` 所以这我们就暂时不改了啊
`44:17` 等下一次再提交的时候就可以给他改掉了啊
`44:20` 这边要去执行命令
`44:21` 那我们来允许一下啊
`44:22` 他需要把我们当前这个啊文件夹给他创建了
`44:25` 然后然后呢再来去形成新的通行证
`44:28` 说白了就是清理旧的通行证
`44:29` 去形成新的通行证
`44:31` OK现在又重新用那个叫git commit edit去干活了啊
`44:35` 那这里面是要创建通行证的文件夹来允许一下
`44:38` OK单元测试这个pass已经有了
`44:40` Yes
`44:40` 允许写入啊
`44:41` 单元测试的这个pass已经出来了
`44:45` 好现在的话又是刚刚挂掉的地方
`44:48` 就是我们的质量检查啊
`44:50` 看那个质量检查这一次能不能通过
`45:07` 好OK他的质量检查呢又再一次出结果了啊
`45:11` 但是这一次又是一个失败啊
`45:13` 他告诉我们
`45:13` 其实代码质量中有两个必修复的问题啊
`45:16` 数类型和数据库质量不匹配
`45:18` 他这个程序的错误处理啊
`45:20` 那就是说我们当前质量检查中
`45:21` 还是有一部分问题没有去通过啊
`45:24` 所以说他还是要去再次修复一下
`45:29` 那这个其实也正常
`45:30` 同学们可能会有些好奇
`45:31` 说哎为什么AI上一次没检查出来
`45:33` 这一次又检查出来了
`45:35` 这就跟我们人一样
`45:36` AI他并不是一个说啊
`45:37` 他并不是说一个啊叫做百分百精确的物种对吧
`45:41` 就跟我们人一样
`45:42` 我这一次让你检查这个这这个问题
`45:44` 你没检查出来
`45:45` 但第二次你有可能检查出来对吧
`45:47` 这人我们人都有可能出现这个问题
`45:49` 那A其实也一样
`45:50` 因为现在MM模型的话
`45:52` 其实本质来说
`45:52` 它里面的神经网络
`45:54` 其实有的时候
`45:54` 你可以认为就是靠概率去搞东西的
`45:57` 所以这一次这个概率没有顺上
`45:59` 但下一次这个概率就有可能顺上了
`46:01` 也就是说在上一次这个数据
`46:03` 它可能或者说这一段代码在它的模型里面
`46:06` 它的权重没那么高
`46:07` 他可能就没有仔细去看
`46:09` 但第二次呢他可能就仔细看到了
`46:11` 所以这个是非常非常正常的
`46:13` 所以说我们一次质量不过
`46:15` 但第二次质量通过啊
`46:17` 都是非常正常的事情啊
`46:18` 所以我们现在就让他把我们这个质量问题
`46:21` 来去修复一下就可以了
`46:25` 好OK他两个问题修好了
`46:27` 修好之后的话呢
`46:28` 他就会又重新再来去运行我们的这个edit
`46:31` 然后这个edit又会去运行单元测试和质量检查啊
`46:35` 相当于把我们流程再来重跑一遍
`46:36` 来给个yes啊
`46:37` 允许他去执行
`46:43` 好单元测试呢都通过了
`46:44` 那说明他刚刚的代码改动啊
`46:46` 其实是并没有去影响我们的代码质量好
`46:48` 那就看他后续对我们做整个代码的质量
`46:51` 审查的结果是什么样的了
`47:01` OK可以看到这一次呢
`47:02` 他总算是我们的质量检查呢是yes了
`47:05` 因为我们是否允许写入啊
`47:07` 那肯定是yes
`47:08` 允许你写入这个pass
`47:10` OK那这样的话我们两个检查呢就全部都通过了
`47:14` 那通过之后正常来说他应该会执行我们的git
`47:16` 然后会执行这个叫git commit
`47:19` 然后这个git commit的话又会被hook
`47:21` 也就是钩子所拦截住
`47:22` 然后钩子呢会检查这两个通行证
`47:25` 哎
`47:25` 两个通行证如果都是pass呢
`47:27` 就会放行
`47:27` 所以我们最终来看看能不能放行我们的git提交
`47:34` 好的可以看到啊
`47:35` 我们从他那个日志里面是已经发现了
`47:38` 其实是已经提交成功了啊
`47:40` 已经提交成功了
`47:40` 都最后都已经提交到git意义上了啊
`47:43` 说明他其实已经通过了我们的hook的钩子检查
`47:46` 你看上面有一个对应的日志啊
`47:49` 两个pass呢都被读取了出来
`47:51` 然后所以说我们当前这个钩子呢就会通过了啊
`47:53` 钩子通过就git提交好
`47:55` 那最后他要去执行一个叫做删除命令
`47:58` 说白了就是把我们这两个旧的通行证删掉
`48:01` 因为我们说过提交成功
`48:02` 要删除这个旧通行证的来
`48:04` Yes
`48:05` 允许一下
`48:06` OK这两个旧通行证就被删除掉了
`48:10` 好了
`48:10` 可以看到all down全部都完成了
`48:12` 那么至此我们这个提交呢就算是成功了
`48:15` 那所以至此的话
`48:16` 我们也验证了我们当前所完无所配置
`48:18` 这个钩子的工作啊
`48:19` 那其实这个钩子的工作主要就是拦截了一下
`48:22` 通过cloud去进行了一叫git commit
`48:25` 我们的要求就是代码不能随便提交
`48:28` 也就是说你的代码不能随便给我存档
`48:30` 我们的存档都是好东西对吧
`48:33` 以后我都把代码噼里啪啦改改乱了
`48:36` 改崩了
`48:36` 我要回退到一个正常的存档上的
`48:39` 所以说我们从现在开始就加了验证
`48:41` 但凡你能存档
`48:42` 就必须经过单元测试以及质量检查
`48:46` 你通不过去
`48:47` 我就不让你存
`48:48` 你就不你就你就需要让AI不断的给我修修修
`48:50` 修到可以存为止
`48:53` 哎这就是我们通过加钩子去加判断啊
`48:56` 满足或不满足啊
`48:57` 都有对应的动作啊
`48:59` 所以这样的话就可以确保我们整个代码
`49:00` 在工程化
`49:01` 在质量上就有了一个保证
`49:03` 同时呢我们也通过这个方式呢
`49:05` 去把整个code code里面的sub edit也是子I进
`49:09` 都给它应用了一下
`49:10` 你看到我们这个EDG还比较多
`49:11` 总共搞了三个
`49:12` 一个是git提交的
`49:14` 一个是质量检查的
`49:15` 一个是单元测试的啊
`49:17` 各有各的作用啊
`49:18` 各有各作用
`49:18` 好的
`49:19` OK那么至此
`49:20` 我们就完成了关于hook钩子的一个配置
`49:23` 以及我们git提交的一个质量控制好的
`49:27` 那么让我们保存一下视频
`49:28` 下一个视频再见