# CLAUDE.md — Jerry的知识库使用说明书

> Claude 每次新对话自动读取此文件。
> **知识维基系统详见 [[wiki/README]]，维基操作前先读 workflow。**

## 我的 Vault 约定

- 所有笔记用 Markdown，有 YAML frontmatter（tags, created 字段）
- 链接用 Wiki 语法 `[[]]`
- 复盘每天 3 个问题：今天学了什么、哪里卡住了、明天第一件事做什么

## 知识维基系统（基于 Karpathy LLM Wiki 模式）

### 三层架构

| 层级 | 说明 | 对应目录 |
|------|------|---------|
| **原始资料** | 不可变，AI 读取不修改 | `raw/`（含 Clippings/、学习日志、抖音视频等子目录） |
| **维基** | AI 生成维护的页面 | `wiki/`（含概念、学习系统、自我认知、核心目标、AI学习等子目录） |
| **模式** | 本文件 + 工作流指南 | CLAUDE.md、[[wiki/.workflow]] |

### 操作路由

| 触发句 | → 执行规则 |
|-------|-----------|
| "摄入 [文件]" 或 "处理 [文件]" | 读取[[wiki/.workflow]] → 按 Ingest 流程执行 |
| "查询 [主题]" 或 "维基里关于 [主题]" | 先读 index.md → 找到相关页面 → 读取 → 综合回答 |
| "巡检" 或 "健康检查" | 按工作流指南 Lint 流程执行 |
| "把这个答案存为页面" | 将刚才的综合回答保存为维基新页面，更新 index 和 log |

### Ingest 标准流程（执行顺序）

```
1. 读取用户指定的原始资料
2. 和用户讨论 2-3 个关键要点（问用户想侧重什么）
3. 如有需要，在维基建摘要/概念页（输出为 Markdown，带 YAML frontmatter）
4. 更新维基中相关的已有页面（补充、修正、交叉引用）
5. 更新 wiki/index.md（添加新页面条目）
6. 在 wiki/log.md 追加条目
```

### 维基页面格式规范

```yaml
---
tags: [相关标签]
created: YYYY-MM-DD
aliases: [别名]
---
- 一句话定义
- 核心要点（3-5 条）
- 关联知识 [[Wiki 链接]]
- 来源：[[原始资料文件名]]
```

### 索引与日志维护规则

- **index.md**：按类别组织，每页一行链接 + 一句话摘要。每次摄入/新增页面后更新
- **log.md**：只追加。格式 `## [YYYY-MM-DD] 操作类型 | 标题`

## 技能系统

> 所有技能由 Claude Agent 根据对话内容自动匹配触发，无需手动路由。

| 触发句 | → 执行规则 |
|-------|-----------|
| "检测我" / "考考我" / "quiz me" / "test me" | 自动匹配 `knowledge-check` skill，执行跨学科理解检测 |
| "复盘" / "总结对话" / "journal this" / "分析这次对话" | 自动匹配 `conversation-journal` skill，执行对话复盘分析 |

### 工作原理

Claude Agent 自动根据对话内容匹配 `.claude/skills/` 下的技能描述，直接加载执行。

### 管理技能

→ `.claude/skills/` 目录下的各 SKILL.md 文件
